# 2026-08-13 工作日志

## G2-R2 fit×margin 冲突收敛（只读）

- 用户提出「应用层 contain-fit 到 inner-paper-area + 边距 → Sumatra noscale」解决 fit/margin 冲突。CodeReview 核实代码：margin-bake(`main.js:569-604`) 与 bake(`main.js:532-568`) 路径**已用 `scalePolicy:'none'`+应用层几何**；唯一缺口=纯 source 路径(`main.js:605`)仍 Sumatra fit。`placement-bake-processor.js:99` 明写「fit 会二次变换」。
- 结论：方案**不推翻 D2**，反而坐实单一几何权威；32-case Truth 纯化为 `{orientation,rotate}`，`noscale` 是 Margin 常量（消解 F2/F3）；命名 Fit-to-inner-paper-area（先 inset margin → 先 rotate 后 contain-fit）。
- 风险 R1 `pdfMargin.process` 对超大 PDF 是否真 contain-fit 须读 `pdf-margin-processor` 确认；R2 不双重边距；R3 旋转顺序；R4 OFD；R5 与 bake 互斥。
- 报告 `c2g-r2-fit-margin-resolution.md`（commit 待分配）。冻结：`e23107b`/`c39ae14` 保留；implementation 未批准；唯一 blocker=T5 物理复核。未改生产代码。

## Gate 1 代码级核实（本回合新增）

- 主动读 `scripts/margin_contract.py`(`apply_pdf`/`apply_margin_contract`/`contain_fit`) + `add-pdf-margins.py` 兼容壳。**Gate1 代码级 PASS**：统一几何引擎**已存在**且正是 Fit-to-inner-paper-area——`contain_fit`(L72-96) 是真正 contain-fit(scale=min(1,sx,sy),居中,scale≤1 不放大,oversized 自动缩)；`apply_pdf`(L240-321) 输出 MediaBox=目标纸(G-2)、/Rotate=0(R-1,旋转烤进相似矩阵)、content_rotation 在 fit 前参与(θ%180==90 content 宽高互换)；旧「页面外扩」模型已删。原担心「clip 要另建引擎」排除。
- 🔴 **R6 双重交换陷阱**：`policy_a`(L51-69) 按「源纸 dims + content_rotation 推导输出方向」(θ%180==90 输出纸 swap)；32-case Truth 把 `orientation`/`rotate` 当独立轴。直接传「已交换目标纸 dims + rotate」会在 θ%180==90 再 swap=双重交换(landscape+rot90→回 portrait)。须翻译层(传原生纸 dims+Truth.rotate 让 policy_a 推导,orientation 作校验)，A/B 实验钉死。
- 🔧 **集成缺口(G1a/b/c)**：纯 source(`main.js:605`)不调引擎→Sumatra fit；`main.js:583` 调 `pdfMargin.process` 漏传 `paperW_mm/H_mm`(回退源 MediaBox 不交换纸)；漏传 `content_rotation`(恒0,Truth.rotate 未入几何层)。真实工作量在接线，非新引擎。
- 文档 `c2g-r2-fit-margin-resolution.md` 已更新：加 §4.5 Gate1 核实、R1 改代码级确认、R6 双重交换、§7 改 3-Gate 正式序列、§8 改「接线非新引擎」。本回合 commit 待分配。未改生产代码。

## G2-R2 架构冻结（用户终审 2026-08-13 末）

- 用户终审修订：把「Sumatra fit」与「应用层 contain-fit + noscale」**强制分命名**（禁共用 `fit`）；最终管线 = 32-case Truth `{orientation,rotate}` → Geometry Translator → `margin_contract.apply_pdf`(MediaBox=纸,/Rotate=0) → Sumatra `noscale`。
- 代码级确认 `apply_pdf` 已实现该管线：`contain_fit`(L72-96) 的 fit 目标 = `paper - margin` 的 **inner paper area**（非整纸、非 clip），与用户「Fit 目标从来是 inner area」精确一致 → Gate1 绝非假设。
- **R6 双重交换收敛为精确 Translator 公式**（不再只是待观察陷阱）：`apply_pdf` 无 orientation 参数，输出方向由 `policy_a(native, contentRotation%180)` 唯一推导；translator 规则 `nativeOri = (rotate%180==90)? swapped(orientation) : orientation`，orientation 只一次性决定 native 指派、policy_a 做唯一 swap、绝不二次传 orientation。用 T5/Gate2/反例三行自检通过。
- 32-case 与 margin 彻底解耦（margin=常量 3mm、scale=noscale 由 Margin 层注入，Truth 不录 fit/scale）→ `Rotation Truth ≠ Margin Truth ≠ Printer Execution` 架构成立。最终命名替代「Truth→SumatraCommand」。未改生产代码。
- 文档 `c2g-r2-fit-margin-resolution.md` 加 §9 冻结语义模型（§9.1 命名纪律 / §9.2 管线 / §9.3 代码事实 / §9.4 Translator 公式 / §9.5 解耦 / §9.6 命名）。MEMORY.md 超 injection 上限已压缩为 C-2 终态索引。

## G2-R2 Geometry Authority 收敛（用户终审 2026-08-13 末，接 §9）
- 用户 8 点提案逐点代码核实：#1/#2/#3/#5/#6/#8 已被 `apply_pdf` 满足（单一 CTM、无中间 PDF、scale≤1、pt-native、Margin 不重判 Truth、INV-M1..M10）；#4 margin 对称展开适配器与 #7 几何黄金测试集为新增。路线裁决=保留 apply_pdf 晋升为唯一 Geometry Authority、消灭纯 source Sumatra fit、统一 noscale。文档加 §10（逐点核实表 / 单一 CTM 确认 / INV-M1..M10 / 黄金测试集 spec / 路线裁决）。未改生产代码。

## G2-R2 黄金向量集落地 + 实跑通过（2026-08-13 末）
- 用户收口：停止寻找新 margin 算法，保留 `apply_pdf` 为唯一 Geometry Authority，下一步只验证 Translator + 接线 + 黄金几何向量。黄金集严格分两层：A 几何引擎 / B Translator。
- 落地：`docs/c2g-r2-golden-vectors.json`（Layer A 5 例 + Layer B 8 例 + 负向控制，hand-derived expected，独立于 Gate-1 几何契约 JSON 以免污染其 schema）+ harness `frontend/test/printGate/marginContract/geometry_golden_vectors.py`（backend venv+pikepdf 10.10.0，只读 import 引擎）。
- 实跑：对**未修改** `margin_contract.py` ALL PASS（Layer A 80 检查 + Layer B 9 检查）。Layer A 确认 A4-rot90 MediaBox 交换为 landscape（先旋转后 fit）、A5-rot180==A1（180°保 bbox）；scale 34/35、1、0.4819，四边≥3mm，单一 cm/Do。Layer B 8 例无双重 swap，T5 candidate(landscape,180) 收敛 landscape，负向控制证伪 naive 双重交换。
- 范围：仅证几何+Translator 链自洽，不替代 Gate2(A/B 物理)/Gate3(T5 真机)。未改生产代码。本回合 commit 待分配。

## G2-R2 生产链接线审计（只读，2026-08-13 收尾）
- 用户点明最大风险=真实生产链接线（非底层几何）。审计三路径分派（main.js:528-607）：A 纯source(Sumatra fit,绕过引擎) / M margin(add-pdf-margins→apply_pdf+noscale) / B bake(placement_bake→apply_pdf+noscale)。
- **引擎身份确认**：add-pdf-margins.py L51 `from margin_contract import apply_pdf` → golden 测的是真引擎，可信度成立。
- 缺口精确定位：G1a 纯source绕过(main.js:605+print-settings.js:188 默认 contain)；G1b main.js:583 漏传 opts.paperW/H_mm→add-pdf-margins 兜底源MediaBox（Gate2 case 源=目标巧合正确）；G1c pdfMargin.process 无 content_rotation 槽位→旋转断链；**G1d 无离散 Translator 层**（§9.4 设计未落地，bake 路径隐式靠 buildPrintExecutionPlan 一次性算 contentRotation+native paper，source/margin 路径缺失，R6 防护无从落地）。
- Gate2 最小接线方案(§5，未实施)：路由入几何路径+G1b 补传 resolvePaperMmFromSettings+G1c 加 contentRotation 参数透传+G1d 落 Translator 公式+维持 noscale；单变量纪律。T5 180° 仍 candidate。
- 文档 `c2g-r2-wiring-audit.md` 新建。未改生产代码。

## G2-R2 实现就绪审计（只读，2026-08-13 收尾后）
- 用户正式关闭「找更好 margin 算法」；设 G2-R2 状态：Geometry/apply_pdf/Golden/Wiring ✅，G1a/b/c/d 🔴 OPEN，Gate2 NEXT、Gate3 AFTER。要求做 implementation-readiness audit 收缩 change set。
- 溯源 Truth：`orientation=ps.paperOrientation`(PrintService.js:79)、`rotate=ps.sourceRotation`(PrintService.js:69)。分派 main.js:527-607：bake(L532冻)/margin(L569)/pure-source else(L605)。默认 3mm→Gate2 case 落 margin 路径已走 apply_pdf→**G1a 不触发，延后 D1**。
- change set：G1b main.js:583 补 opts.paperW/H_mm（resolvePaperMmFromSettings:207 已存在）；G1c pdfMargin.process 加 contentRotation 槽位→CLI --content-rotation 已支持(add-pdf-margins.py:191)；G1d 新建 geometry-translator.js(§9.4)+单测对齐黄金 B 向量。noscale 已就位(main.js:600+print-settings.js:301)。margin_contract/bake/16表 全不改。
- 🔴 致命：`normalize()`(print-settings.js:172) swap 准则(requestedOrient≠naturalOrient)≠§9.4(rotate%180==90)，复用会双重 swap→**G1d 必须全新独立，禁复用 normalize**。
- Deferred Ledger：D1 G1a 全量纯source路由 / D2 margin 对称展开 / D3 Translator 接 bake / D4 sourceRotation 命名域统一。文档 `c2g-r2-implementation-readiness.md` 新建。未改生产代码。

## G2-R2 终态裁决冻结（用户收口，2026-08-13 末）
- 用户终审：不再优化 `apply_pdf` 算法（已是单CTM+Form XObject+inner-area contain+scale≤1+/Rotate=0 最优）。唯一可优化=margin 表达/调用边界/几何阶段数，非换算法。
- 写入 `c2g-r2-wiring-audit.md` §7 终态裁决：① margin 属 Paper Geometry Contract(`margin_mm=3`→`expandMarginSymmetric`→LTRB 仅引擎内部；3mm=physical paper inset) ② 无中间 PDF(已满足) ③ margin 非独立步骤(`fitTarget=Paper-margins`) ④ 几何权威收回 App(Sumatra 仅 executor) ⑤ 守 `noscale≠物理3mm`(打印机 unprintable area=D2 physical precondition) ⑥ 三层准确性 ⑦ Translator 单入口(Margin 层永不知 orientation,根治 drift) ⑧ 优先级 G1a/b/c/d ≫ margin_contract。
- 决策表封死回潮候选：先fit满纸再加margin / Sumatra fit+PDF margin / 中间PDF / rasterize / margin后fit / 重写fit engine 均 ❌；apply_pdf 一次完成 / 单CTM+Form XObject / noscale / Translator 单入口 均 ✅。
- MEMORY.md C-2 段加终态裁决索引。未改生产代码。

# 2026-08-12 工作日志

## C-2-G · INV-R 零代码预测实验（已完成，未改生产代码）

- 5 项预测全 PASS（8 组合 × 2 纸型），INV-R 违约链因果闭环完成。
- P-DELTA 8/8：bake 实际旋转 ≡ layoutRotation，contentRotation 在 bake 内被完全丢弃（CTM 产物矩阵直接证实，非仅源码阅读）。
- 🔴 新增发现：几何缺陷是**双纸型**，不止横纸。`cr∈{90,270}` 时 bake contentBox = transpose(placedRect)，内容溢出触边裁切。竖纸 cr=90 实测面积 78.9%→55.3% 且触边 → **推翻「竖纸零回归」**（此前只在 cr=0 子空间验证）。`main.js:555`「bake 已含旋转」注释为事实错误。
- E-SIM 8/8 预演方案 E（等价输入 `layoutRotation:=cr+lr` 驱动未修改生产 bake）。cr=0 时 BASE vs E-SIM 内容流 CTM 字节级 IDENTICAL（横纸+竖纸零回归）。
- ⚠️ E 改动面修正：不是单处 phi，而是 **E1**（`placement_bake.py` phi 表达式）+ **E1a**（`buildBakeSpec` 透传 contentRotation）+ **E2**（`main.js` executor offset 归一 `sourceRotation: execOrient==='landscape'?90:0`，竖纸收敛到已冻结 golden 命令串）。
- 报告：`frontend/test/printGate/marginContract/c2g-inv-r-prediction-experiments.md`（已本地 commit 0ddce78）。

## 工程事件

- `git push` 在本 sandbox **失败（401 Unauthorized）**：bundled PortableGit 的 GCM 无 GitHub 写凭据；系统 git 不存在、无 `.git-credentials`。`ls-remote` 可读（不需写凭据）。→ 用户需在自己终端手动 push。push 命令：`git -c lfs.locksverify=false push origin HEAD`（单行）。
- 冻结状态更新：C-2-G 由 PAUSED → **E IMPLEMENTATION AUTHORIZED**（非 Gate PASS）。E1/E1a/E2 已实施并提交 `e23107b`。resolver / 16 表 / RotationResolver / paper / noscale / 其他打印路径仍冻结。真机物理打印为终局 Gate。

## E 实施（已批准并执行，commit e23107b）

- E1 `scripts/placement_bake.py` 两处 `phi = (360 + contentRotation + layoutRotation) % 360`（新增读取 contentRotation）。
- E1a `placement-bake-processor.js` `buildBakeSpec` 透传 `placement.contentRotation`（默认 0）。
- E2 `electron/main.js` executor offset 归一 `sourceRotation: execOrient==='landscape'?90:0`（竖纸收敛 golden 命令）。
- 验证（`.out/c2g-e-verify.mjs` + `.out/c2g-e2-check.mjs`）：8 组合 INV-R 8/8 / placedRect 恒等+无触边 8/8 / cr=0 CTM 字节级 ≡ pre-E baseline；E2 LAND→rotate=90、PORT→无 rotate 8/8。
- ⚠️ 仍非 Gate PASS：真机物理打印（implicit −90° 沿用冻结常量未重测）为终局 Gate。

## 后续（配套工作，独立 commit，不混入 E）

- Gate 工程：重构 `bakeLandscapeMatrixGate.mjs` 消除伪绿，truth source 改为真实生产链（resolveContentPlacement→placement_bake.py→真实 bake 产物→Sumatra 执行→最终方向/bbox/fit）；断言改为 INV-R + placedRect 恒等 + 无触边。
- 把 8 组合 BASE/E-SIM 数值固化为回归 golden。
- 16 表显式标注「direct-print model only」。
- 真机物理打印终局确认（C-2-G Gate PASS 前置）。

## 真机 T1~T4 + Paper Direction 只读审计（C-2-G → BLOCKED）

- 真机结果：横纸型×横向 0° 物理 PASS（E 几何/executor 在横纸×横向有效，G3/G4 ✅）；**横纸型×纵向方向（任意 UI rotation）仍 `landscape`，应 140×240 portrait → ❌**。
- 定性：失败不是 E1/E1a 的 contentRotation 问题，而是更上游「纸张方向」未进入 Execution Plan/executor。E1+E1a+E2 **不回退**。
- 根因（只读审计 `c2g-paper-direction-audit.md`）：① 打印链方向仅相对布尔 `settings.landscape`（`usePrint.js:762`/`PrintService.js:74`），无绝对 `paperOrientation` 传到 IPC；② 前端 `resolvePaperSpec`(`paperSpec.js:88-91`) 已算对 140×240 plan.paper，但 electron `normalize`(`print-settings.js:199-205`) 重算 `paper.orientation` 时丢弃 `executionPaper.orientation`、回退 `naturalOrient`(横纸型=landscape)；③ 源单文件路径 `executionPaper=f?.paper`(`usePrint.js:954,728`) 未从 plan 写回 → 多为 null。
- Q3 边距：preview/Plan 在「纸张原生维度」坐标系（横纸型恒 240×140）算 margin（`previewState.js:182-199`）；方向交换未传到执行器 → 横向对、纵向错；与 Q2 同一上游错误，不能单独修 margin。
- 状态：`C-2-G = BLOCKED`（G2 Paper Direction）。G2 修复面=`normalize` 以 executionPaper.orientation 为权威（或新增绝对 paperOrientation）+ 源单文件传 plan.paper + UI 方向语义升级绝对；与 E 正交独立 commit。冻结边界（16表/resolver/RotationResolver/paper/noscale/其他）不动。

## G2 零代码预测实验（已完成，未改生产代码）

- 脚本 `.out/c2g-g2-predictions.mjs`：调用**未修改** `resolvePaperSpec` / `normalize` / `buildPrintSettings`，横纸型建模为 `customPaper 240×140`（规避 PaperRegistry require）。UI rotation 隔离为 0（纯 G2）。
- 结果：横纸型+横向 → Plan✅/当前链单一Truth✅/G2-SIM✅；横纸型+纵向 → Plan✅(140×240 portrait)/**当前链断裂❌(仍 240×140 landscape)**/G2-SIM✅。
- **关键发现**：`normalize`(L202-203) **已原生读取 `src.paperOrientation`**，只是前端 `PrintService.buildPrintSettings` 从不发该字段 → **G2 最小修复 = 纯前端补传 `paperOrientation: requestedPaperOrientation(userSettings)`**，零 electron 改动即可闭环单一 Paper Truth（G2-SIM 实证）。
- **第二个独立缺陷**：`normalize` 完全不读 `src.executionPaper`（两 case 实测均丢弃）→ 即便前端透传 Plan truth 也没用。
- Q3 边距实证：usable rect 纵向下转置（Plan 134×234 vs 当前 234×134），确认边距错位是 G2 同一上游错误的第二表现，非独立 margin bug。
- P-G2-DELTA 因果闭环：expected=portrait/140×240，actual=landscape/240×140，delta=绝对方向未进 IPC + landscape 布尔无法表达横纸型×纵向。
- 报告：`frontend/test/printGate/marginContract/c2g-g2-prediction-experiments.md`（待独立 commit）。`e23107b` 保持有效，E1+E1a+E2 不回退。

## G2 解冻边界锁定（用户裁决，未实施）

- 状态锁定：**C-2-G = BLOCKED / G2 Paper Direction**；G2 **等待批准解冻**，e23107b 保留、E1+E1a+E2 不回退。
- 锁定唯一解冻项：`frontend/src/services/PrintService.js` `buildPrintSettings()` 补传与 Plan 同源绝对 `paperOrientation: requestedPaperOrientation(userSettings)`（单一来源链，避免三变量混回）。
- 明确不解冻：electron/print-settings.js、`usePrint.js` executionPaper 接线、`executionPaper.orientation` 消费、`placement_bake.py`/`placement-bake-processor.js`/`main.js`、resolver、16 表、RotationResolver、margin contract、noscale、其他路径。
- 验收（实施后严格）：8 组合数值回放 + G2 delta 单一 Paper Truth + E regression（cr=0 字节级 + 8/8）+ 真机 T1~T4（重点 240×140↔140×240 交换、边距随坐标系交换）。本轮不碰代码。
- 结论印证：margin 异常 = Paper Direction 未交换坐标系的派生症状，无理由单独改 margin / 回退 E。

## G2 实施（已批准并执行，commit c39ae14）

- G2-1 `frontend/src/services/PrintService.js` `buildPrintSettings()`：import `requestedPaperOrientation` + 补传 `paperOrientation: requestedPaperOrientation(userSettings)`（与 resolvePaperSpec 同源，单一来源）。零 electron 改动。
- 链路确认：前端 `ps` → IPC `settings` → `main.js` 透传（:521,611）→ `backend.print` → electron `buildPrintSettings`(:277 normalize) → `normalize` 读 `src.paperOrientation`(:202-203)/回写(:226)，字段不被丢弃。
- 验收（`.out/c2g-g2-verify.mjs` + `.out/c2g-e-verify.mjs` + `.out/c2g-e2-check.mjs`）：Gate1 仅此文件/来源正确 ✅；Gate2 8组合 Plan==IPC==normalize==dims 单一 Paper Truth 8/8（横纸×纵向→140×240/usable 134×234，无 240×140/234×134 转置；UI rotation 不影响纸方向）✅；Gate3 E regression（INV-R/几何/cr=0字节级/E2 command shape）全不退化 ✅。
- 状态：**C-2-G = G2 IMPLEMENTED / 待数值+真机 Gate**（非 Gate PASS）；e23107b 保留不回退；冻结边界（electron/usePrint/executionPaper/margin/16表/resolver/RotationResolver/noscale）不动。
- 待用户：真机 T1~T4（重点 T2 横纸×纵向 140×240、T4 竖纸×90° 内容顺时针+不裁切）终局确认。

## G2-R1 镜像回归只读审计（portrait-native + landscape 新 blocker）

- 用户裁决：升「竖纸型(原生竖向) + 横向方向 = 裁切」为 G2 新 blocker；先零代码定位「谁仍持旧 portrait geometry」再定 G2-2；不回退 c39ae14；不宣布 G2 PASS。
- 零代码实验 `.out/c2g-r1-mirror-experiment.mjs`（import 真实 `resolvePaperSpec`/`requestedPaperOrientation`/`normalize`）：四象限 Plan==normalize 全 PASS。**关键读数：竖纸×横向 下 c39ae14 对 normalize 完全惰性**（landscape=true 早已走 'landscape' 分支，paperOrientation:'landscape' 与之同值）→ 不改变该组合行为；横纸×纵向 下 legacy=240×140❌、G2-1=140×240✅（c39ae14 唯一改变的组合 = G2 主修复）。
- 逐层追踪：Plan/resolvePaperSpec ✅297×210；buildPrintExecutionPlan ✅；buildPrintSettings(IPC) ✅ paperOrientation='landscape'；normalize(source 轨) ✅297×210→"landscape,fit,paper=a4"，fit 不裁切；canvas 轨 computePaperLayout 故意产 portrait-native 210×297(swap 推迟到 buildRenderCommand，Policy B 设计，非 bug)，effPaperRect 交换后 297×210 不裁切。
- 结论：**当前活跃路径无一「持旧 210×297 且裁切」；c39ae14 非竖纸×横向裁切根因**。唯一结构风险 = canvas 轨交换权威(`forcedLandscape`/`documentState.*Orientation`, usePrint.js 全文 grep buildRenderCommand/paperOrientation=0 命中) 与 source 轨 `normalize` 是**两套独立来源**（历史未统一），但二者对竖纸×横向都应='landscape'，正常不分叉。
- 行动项：① 不回退 c39ae14（与裁决一致）；② **向物理 Gate 协议追加 T5=竖纸(A4)×横向**（T1~T4 缺此组合，从未真机闭环）；③ 不宣布 G2 PASS；④ canvas 轨两权威不统一为独立结构债（不混入 G2）。
- 报告：`frontend/test/printGate/marginContract/c2g-r1-portrait-landscape-mirror-audit.md`（只读，未改生产代码）。

## G2-R1 结论收紧 + T5 物理 Gate 协议（用户裁决，未改生产代码）

- 用户收紧：c39ae14 在「竖纸×横向」下 change-delta=0（完全惰性）→ **已证非该组合裁切触发修改**；但「是否早于 c39ae14」**不能断言**（缺该组合修改前后真机 A/B 证据）。审计报告结论已从「根因必早于 c39ae14」收紧为「非触发已证 / 早于与否 UNKNOWN」。
- 批准 T5 = 竖纸 A4 × 横向（297×210，0°/3mm/裁切0），补齐纸型×方向 2×2 矩阵第四象限（T1/T2/T3 已覆盖其余三象限；T1~T4 原不含此组合，从未真机闭环）。
- 决策树：T5 PASS → C-2-G PASS（关闭 G2-1 致竖纸横向回归怀疑）；T5 FAIL → C-2-G BLOCKED，新 blocker 命名 **G2-R2 / Canvas Paper-Direction Authority Divergence**（不回退 c39ae14）。
- 架构判断：source 轨 normalize 与 canvas 轨 forcedLandscape/documentState.*Orientation 是两套方向权威（未统一），当前非 blocker（数值未证分叉），T5 FAIL 优先怀疑此处。
- 明确否决清单：revert c39ae14 / 改 print-settings.js / usePrint.js / executionPaper / margin / RotationResolver / 16表 / 猜测性改 forcedLandscape / 因 T5 未测标 G2 FAIL。唯一动作 = 跑 T5 真机。
- 物理 Gate 协议 `c2g-physical-print-gate.md` 重写：状态行 / §3 矩阵(3.1 2×2 + 3.2 旋转正交 + 3.3 T5 规格) / §7 T5 分流 / §8 两套方向权威 / §9 否决清单 / §10 G2-R2 分流。审计报告结论同步收紧。均未改生产代码。

## T5 真机 FAIL + G2-R2 只读因果审计（C-2-G → BLOCKED，未改生产代码）

- 真机裁决：**T5 = 竖纸 A4 × 横向 ❌ FAIL**；纸张物理方向 ✅ 已正确 297×210（Paper Direction 已生效）；内容反向 90°，成品逆时针转 90° 才正立。→ 问题从 Paper Direction 缩小到 **Content/Render Orientation**。
- 冻结：`e23107b`(E1/E1a/E2) 与 `c39ae14` 均保留，不因 T5 FAIL 回退；现在不动代码、不进 RotationResolver/margin/Electron `normalize`。
- **R2-1（确认路径）**：`usePrint.js:1051` `createPrintPlanInput(files,settings,fileRotations)` 未传 placements → `buildPrintExecutionPlan.js:287` slot.placement=null → `deriveSourcePrintJobs.js:48` 透传 null → `PrintService.js:90/94` placement/executionPaper=null → `main.js:530` `hasPlacement=false` → 落 `:605` 纯 fit **else 分支**。**T5 走纯 source/Sumatra/fit，不经 bake、不经 canvas**（推翻「canvas 双权威分叉」原假设）。
- **R2-2（rotation ledger，含 ±）**：UI landscape + 0° → `paperOrientation='landscape'` → `normalize` 297×210 landscape / contentRotation=0 → `print-settings.js:292` `if(contentRotation!==0)` 跳过 rotate → 命令 `landscape,fit,paper=a4`（无 rotate）→ Sumatra `-landscape` 隐含 **−90°** → 净内容角 **−90°** → 反向侧躺。用户「逆时针 90° 修正」= 当前偏 −90° 体系，吻合。
- **R2-3（根因）**：根因 = **source/fit 单文件路径对「portrait 发票×landscape 纸×0°」缺内容方向补偿**；理论补偿 `ROTATE_MATRIX[portrait][landscape][0]=270`(`sumatra-command-resolver.js:37`) 但 `resolveSumatraRotation` electron 从未调用；E2(`main.js:564`) rotate=90 补偿只在 bake 分支、对单文件不触发。三轨对照：bake 正常 / canvas 未触及 / **source/fit 为根因**。
- G2-R2 实质收敛：从「Canvas 双方向权威分叉」收窄为「source/fit 单文件路径 portrait×landscape×0° 内容方向补偿缺失」（标签 G2-R2 保留，范围收窄）。canvas 双权威仍为独立结构债但非 T5 触发层。
- 修复边界（仅记录不执行）：候选点 `print-settings.js:292` 短路；单变量纪律——只针对 portrait×landscape×0° 补 rotate，不碰 e23107b/c39ae14/RotationResolver/margin/normalize/16表/canvas 双权威；修复后以 T5 真机 + E regression + 横纸×纵向 G2 数值为 Gate。
- 报告：`frontend/test/printGate/marginContract/c2g-r2-content-rotation-causal-audit.md`（只读，未改生产代码）。状态机：`C-2-G = G2 IMPLEMENTED(数值四象限 PASS) / T5 真机 FAIL / G2-R2 OPEN READ-ONLY`。

## 32-Case Print Command Truth Matrix + 一致性审计（只读，未改生产代码）

- 用户整合 32 项实测最终打印命令，提议 G2-R2 从「推导旋转算法」转「冻结 32-case Truth → 一致性审计 → 最小 `PrintCommandTruthResolver`(PrintState→SumatraCommand) → G2-R2 impl」。架构方向 reviewer 认可；纪律：paperType 与 paperOrientation 两独立维度、Margin 独立成层、Preview 不持 Truth Table。
- 4 轴模型 `invoiceOrientation × rotation(4) × paperType(竖向/横向纸张类型) × paperOrientation(2)` = 32。两 16-case 子矩阵：Table A=横向纸张类型(rotate∈{90,270}) 与既有 `ROTATE_MATRIX` 完全一致（electron 从未调用）；Table B=竖向纸张类型(A4 等, rotate∈{0,180}) 新测。
- 一致性审计结论：① 覆盖 32/32 ✅；② 等价 Case 正常属性；③ 旋转闭环 ✅(mod-180 分组自洽)；④ **跨矩阵不变量 Table A = Table B + 90°（逐格恒定偏移）**——强证据数据非随机。报告 `c2g-r2-32case-truth-matrix-audit.md`。
- 🔴 **T5 值修正（§五错配，180=candidate 非 frozen）**：§五把 T5「横向纸张」当横向纸张类型→rotate=270；T5=竖纸 A4(竖向纸张类型)×横向方向，按 4 轴模型查 **Table B** → **candidate**=`landscape,rotate=180,fit`（非 270；270 是横向纸张类型值，270−180=90 恰为跨矩阵 +90° 偏移，自洽）。**关键纪律（用户裁决）**：180 来自 Table B + 跨矩阵不变量**一致性推导，非独立真机验证**；T5 恰是唯一已知物理 FAIL 的 Case，故 180 在真机复测 PASS 前**仅为 candidate truth，不得冒充 frozen physical truth**。原则=物理实测→Truth→矩阵一致性→冻结（不可反向）。下一步=**单一物理实验** T5(A4/verticalPaper/portrait/0°/landscape) 试 `landscape,rotate=180,fit`：PASS→冻结 180 并推进 32-case→resolver→G2-R2 impl；FAIL→不强行维护 +90° 不变量，Table B 该格/整张标 partial 并按实测重测。
- 冻结：`e23107b`/`c39ae14` 保留；未改生产代码。下一步=冻结数据→设计 resolver→G2-R2 单变量 impl→32-case 真机 regression。

## G2-R2 状态分叉：物理 Truth 驱动（用户裁决，未改生产代码）

- 确认收紧结论正确，正式把状态分三层：① 32-case 实测数据(作为 Truth candidate 数据源) ② 矩阵一致性(Table A=Table B+90° 逐格，强证据但非物理证明) ③ T5=verticalPaper/portrait/0°/landscape→candidate `landscape,rotate=180,fit` 但 **FROZEN PHYSICAL TRUTH=UNKNOWN**。
- **Reviewer 收紧**：Layer① 实为 measured/candidate dataset，非「32 个 frozen truth」；**Frozen Truth Matrix 是 Gate 的产出而非输入**；勿把「已测量」等同「已冻结」。T5 是唯一已知 FAIL 格。

## fit × margin 关系审计（只读，未改生产代码，2026-08-13）

- 用户提议「把 fit 作为 32-case Truth 一部分落实，并据此重审 Margin Contract 的 noscale 前提」。**Reviewer 推翻前提**：D2 `noscale` 是无条件硬冻结(§0/§4)，非「前提于 margin≥硬件边」——该检查属独立 Capability Guard(§5.1,只放行/告警/阻断)。fit 被禁因必按 printable area 再缩 96–98% 破严格边距+第二 scale 解释点(§2.3/§2.5)。
- 🔴 **F2**:32-case 表只录 orientation+rotate,scale 维度从未逐格实测→「all 32=fit」是 assertion 非 Truth。🔴 **F3**:即便采信 fit 也直接违反 D2,须走 §11 契约变更(推翻 D2+更新 vectors+双侧执行器),Resolver 无权覆盖。🟡 **F4**:当前 fit 可能掩盖 desync(§2.4);修正实验=`landscape,rotate=0,noscale`(只改 rotate 对齐 Truth,scale 维持 D2),非用户误提的 `landscape,rotate=0,fit`。🟡 **F5**:scale 策略不归 Resolver 拥有,属 Margin Contract(D2),Resolver 只引用不裁定。
- 确认仍正确:旋转⟂安全边距两层分离、production 未消费 32-case Truth、不在 print-settings.js 堆 if、31 格可作 candidate、T5 唯一未冻结。报告 `c2g-r2-fit-margin-audit.md`。未改生产代码。
- Gate 顺序(正确)：32-case实测→Truth Matrix→一致性审计→发现矛盾→单独物理复核→Frozen Truth→Resolver→implementation。错误：已有矩阵→数学关系→推导值→认为物理正确（T5 正是后者危险案例）。
- Resolver 边界：只答「确定 PrintState→Sumatra 最终命令」{orientation,rotate,fit}，不得知 PDF/Canvas/Preview/Margin/placement/Invoice/RotationResolver。第一版直接硬编码两子矩阵(verticalPaper rotate∈{0,180}/horizontalPaper∈{90,270})，不做公式压缩。
- Margin 独立成层：旋转正确性 ⟂ 安全边距正确性，G2-R2 后只剩 Margin Geometry + hardware safe margin 验证。
- **当前唯一正确动作 = T5 单变量物理实验**：固定 verticalPaper/portrait/0°/landscape，仅把最终命令 landscape,fit→landscape,rotate=180,fit 看成品。PASS→180 升 frozen 并推进 Resolver impl；FAIL→不硬维护 +90° 不变量，重测 Table B。
- **项目状态**：G2-R2 已完成因果定位 + 32-case 数据建模，但**尚未进入 implementation**；唯一 blocker = T5 rotate=180 物理复核。e23107b/c39ae14 保留不回退。报告 `c2g-r2-truth-driven-state.md`（只读）。

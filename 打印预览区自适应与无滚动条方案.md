# 打印预览区：永不出现滚动条 + 跟随窗口自适应方案

> 状态：**方案文档（只读调查 + 修复设计），未改动任何源码**
> 调查日期：2026-08-09
> 范围：打印确认弹窗 `PrintConfirmModal` 内的打印预览区（非主文档查看器 `ViewerViewport`）

---

## 1. 目标

1. **打印预览区永远不出现滚动条**（纵向纸张、横向纸张、任意窗口尺寸下均如此）。
2. **预览区跟随窗口大小自适应**：放大/缩小 Electron 窗口时，预览纸面自动重新 fit，始终完整可见、居中。

---

## 2. 现状与根因（已逐行核实）

渲染链路（自外向内）：

```
PrintConfirmModal.jsx
└── .pcm-panel--new              ← 固定 960×690px（modals.css:955）
    └── .pcm-body--split
        └── .pcm-preview (flex:2)
            └── .pcm-preview-body
                └── PrintPreviewCanvas.jsx
                    └── .pcm-preview-stage
                        └── .pcm-preview-page   ← overflow:auto（modals.css:1051）
                            └── <svg width="100%" height="auto" flex-shrink:0>  ← 高度只由宽度推导
```

### 根因 A —— 滚动条来源（确定性溢出）

- **`modals.css:1051`** `.pcm-preview-page { overflow: auto }`：唯一直接暴露滚动条的规则（且未做 `::-webkit-scrollbar` 美化，显示系统默认宽滚动条）。
- **`modals.css:1059`** `.pcm-preview-page svg { height: auto }`：SVG 高度 = `容器宽度 × (heightMM/widthMM)`，**完全不参与容器高度约束**（缺 `max-height`）。
- **`modals.css:1060`** `flex-shrink: 0`：即使 flex 布局，也**显式禁止** SVG 收缩以适配剩余高度。

三因素叠加：以 A4 纵向、960×690 弹窗估算，SVG 高度 ≈ 496px，而可用高 ≈ 458px → 溢出 ≈ 38px → 滚动条；滚动条又吃掉 ~15px 宽 → 高度进一步溢出，**形成稳定滚动态**。横向纸（比例小）不溢出，故只在纵向纸出现。

### 根因 B —— 不随窗口自适应

- **`modals.css:955`** `.pcm-panel--new { width:960px; height:690px }`：弹窗为**绝对固定像素**。仅当视口 < 94vw/92vh 时才被 `max-*` 夹取；放大窗口时预览区**纹丝不动**。
- **`PrintPreviewCanvas.jsx` 全组件无任何尺寸测量**：无 `ResizeObserver`、无 `window resize` 监听、无 `useLayoutEffect`、无 fit-scale state。容器尺寸变了也没有代码去重算。
- SVG 尺寸策略是**单轴**的（`width:100%` 只跟随宽度）。SVG `preserveAspectRatio` 默认即为 `xMidYMid meet`（等比缩放、居中、留白），但当前 `height:auto` 让这一能力只在"宽度方向"生效，高度方向从未被约束。

> 参照：主文档查看器 `ViewerViewport.jsx:115-217` 已有 `ResizeObserver + window resize` 双保险 fit 实现；**打印确认弹窗完全未接入**。本方案不改动主查看器（超出范围），只修弹窗预览。

---

## 3. 期望终态

- `.pcm-preview-page` 永不滚动（overflow 锁定为 hidden）。
- SVG 在"宽度×高度"**两个方向同时** contain 适配：纸面始终是**完整、居中、等比**的，纵向/横向纸张都如此。
- `.pcm-panel--new` 改为视口相对尺寸，随窗口缩放（带上下限），预览区随之 reflow，无需 JS。

---

## 4. 推荐方案（CSS 优先，三处改动）

### 改动 1｜`.pcm-preview-page`：滚动条彻底关闭

```css
/* modals.css 约 1046-1053 */
.pcm-preview-page {
  flex: 1;
  min-height: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  /* gap: 8px; ← 单子节点无需，删除避免高度膨胀 8px */
  overflow: hidden;   /* ← 原为 auto：永不出现滚动条 */
  width: 100%;
}
```

### 改动 2｜SVG：双轴 contain（核心修复）

```css
/* modals.css 约 1056-1061 */
.pcm-preview-page svg {
  display: block;
  width: 100%;
  height: 100%;          /* ← 原为 auto：让 SVG 视口跟随容器高度 */
  max-width: 100%;
  max-height: 100%;
  flex-shrink: 1;         /* ← 原为 0：允许收缩以适配剩余高度 */
}
```

**原理**：SVG 默认 `preserveAspectRatio="xMidYMid meet"` 本就实现"等比缩放 + 居中 + 留白"。给它一个**同时具备宽度与高度**的视口（`width:100% height:100%` + 父链 `min-height:0` 让高度确定），meet 便会在两个方向同时生效——纸面始终完整可见、不溢出、不滚动。无需在 JSX 加属性（默认即 meet）；若团队要求可读性，可显式补 `preserveAspectRatio="xMidYMid meet"`（非必须）。

### 改动 3｜`.pcm-panel--new`：弹窗流体化，跟随窗口

```css
/* modals.css 约 954-961 */
.pcm-panel--new {
  /* 原为固定 width:960px; height:690px; */
  width: min(1100px, 92vw);
  height: min(820px, 90vh);
  max-width: 94vw;        /* 保留安全夹取 */
  max-height: 92vh;
  overflow: hidden;
}
```

> 因为整条布局是 flex + 百分比 + 视口单位，**窗口缩放会自动 reflow**：父链高度级联到 `.pcm-preview-page`，SVG `height:100%` 重新计算 → 纸面重新 fit。**无需 ResizeObserver**，满足"最小改动、不改生产架构"纪律。

---

## 5. 可选增强（仅当 CSS 验证不足时，默认不做）

若现场发现某些极端比例下 contain 留白过大或存在子像素裁剪，可参照 `ViewerViewport.jsx:115-169` 给 `PrintPreviewCanvas` 加一个 `ResizeObserver + window resize` 双保险、用 `computeFitScale` 计算显式 `scale` transform。

**但本方案建议先走纯 CSS**——它已能在绝大多数窗口尺寸/纸张方向下满足两个目标，且零 JS、零新依赖、零架构变动，与前面 B1 修复的"不为适配而改生产结构"原则一致。

---

## 6. 明确不改动的范围（边界纪律）

- 不碰 `PrintPreviewCanvas.jsx` 的渲染逻辑 / slot 排版 / `PrintPreviewModel`（除非显式补 `preserveAspectRatio` 属性，属可选）。
- 不碰主文档查看器 `ViewerViewport` / `usePreview` 的 ResizeObserver（已有，超出本次范围）。
- 不引入新 seam / helper / loader。
- 不处理 `canvas.css`（本次会话标记为 X / provenance unknown 的既有 WIP）。
- 不处理前面 B1 遗留的 `doc-facts` 旧线 WIP、PDF 自动化测试债务等独立项。

---

## 7. 验证方案（Acceptance）

手动 / 视觉验收（该弹窗当前无单测设施，故以人工验收为正式标准）：

1. 打开「打印确认」弹窗，进入预览区。
2. **无滚动条**：预览区任意状态下不出现横向/纵向滚动条。
3. **随窗口自适应**：拖动/缩放 Electron 窗口到多个尺寸（小→大→超大），预览纸面始终完整、居中、等比，不被裁剪。
4. **纸张方向矩阵**：
   | 纸张 | 方向 | 预期 |
   |---|---|---|
   | A4 | 纵向 | 完整、居中、无滚动条 |
   | A4 | 横向 | 完整、居中、无滚动条 |
   | Voucher 240×140（原生横向） | 纵向/横向 | 完整、居中、无滚动条 |
5. **回归**：确认其它弹窗（`.xec-table-wrap` 等使用 `overflow:auto` 处）不受影响——本改动仅针对 `.pcm-*`，作用域隔离。

> 可选轻量 DOM 断言（非必须，且不进生产代码）：在浏览器控制台检查 `.pcm-preview-page` 计算样式 `overflow === 'hidden'`，以及 svg `clientWidth/clientHeight` 比例 ≈ 纸张 `widthMM/heightMM`。

---

## 8. 风险与备注

- **contain 留白是预期行为**：纸面在预览区内可能上下/左右留白（letterbox），这是正确的"fit 而非 fill"，不应改为 `slice`（slice 会裁切纸面内容）。
- **超宽/超矮窗口**：预览区变窄时纸面变小但**始终完整可见**，可接受。
- **改动面极小**：实质只动 `modals.css` 三处规则；若采用显式 `preserveAspectRatio` 则再多一处 JSX 属性（无逻辑改动）。
- 与前面 B1 hardening 同源原则：**测试/适配需求不应倒逼生产架构变更**；本次纯 CSS 收尾正是该原则的体现。

---

## 9. 实施状态（2026-08-09 已落地，未提交）

**已按本方案 5G 程序执行，仅改动 `frontend/src/modals.css` 三处，未碰任何 JS / JSX。**

| 改动 | 方案建议值 | **实际落地值** | 说明 |
|---|---|---|---|
| `.pcm-panel--new` width | `min(1100px, 92vw)` | `min(1100px, 94vw)` | 实际采用 94vw，与既有 `max-width:94vw` 一致，弹窗在大屏可更宽 |
| `.pcm-panel--new` height | `min(820px, 90vh)` | `min(800px, 92vh)` | 实际采用 92vh / 800px，略保守以留出系统栏空间 |
| `.pcm-preview-page` overflow | `hidden` | `hidden` | 与方案一致 |
| `.pcm-preview-page svg` | `width:100%;height:100%;max-*;flex-shrink:1` | `width:100%;height:100%` | 实际仅去 `flex-shrink:0` + `height:auto→100%`；`max-*`/`flex-shrink:1` 在父链 `min-height:0` + `overflow:hidden` 下非必需，故精简未加（效果等价） |

**前置核查结论**：纸面预览 SVG（`PrintPreviewCanvas.jsx:341`）无 `preserveAspectRatio` 属性 → 默认 `xMidYMid meet`（等比 contain），方案成立，不会拉伸变形。文件内 L83 的 `"none"` 与 L125 的 `meet` 属其它图标 SVG，与纸面无关。

**收尾取证**：`git diff` 仅含上述三处 `modals.css` 改动；`canvas.css` / `electron/main.js`(doc-facts WIP) / `PrintPreviewCanvas.jsx`(旧线样本) 保持基线 ` M` 状态未动；`git diff --check` 无尾随空白/冲突标记。

**⚠️ 视觉验收待用户执行（本环境无法启动 Electron GUI）**：需实测 A4 纵/横、Voucher 横向、窗口缩放、无滚动条、纸面完整居中。若纯 CSS 实测失败，按 5G-6 停手分析，不引入 JS fit 机制。

**提交**：未提交（遵循本会话"改动不自动 commit"纪律），待用户另行授权。

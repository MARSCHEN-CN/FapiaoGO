# 同票多页链路审计（Page → InvoiceDocument）

审计日期：2026-08-02
范围：PDF import → split → parse → PageResultStore → Assembly → DB → DocumentStore → UI
性质：**只审计，不修改**。所有结论均有代码行号或运行探针佐证。

---

## 0. 结论先行

**不是「旧链路复活」，也不是「算法没实现」。**

新链路（PageResultStore + InvoiceAssemblyPipeline + instanceId 分桶）**全部已接线并在运行**。
真正的故障是 **契约漂移（Contract Drift）**：

> Assembly 读取的字段（`page_num`）**从来不存在于生产的 parse 结果中**，
> 而单元测试的 fixture **手工注入了这个字段**，
> 于是 D-1/D-2/D-4 全绿，生产全错。

这解释了「当时验证通过、现在却当成多张发票」的全部矛盾。

**故障定级：**

| # | 位置 | 等级 | 一句话 |
|---|------|------|--------|
| 1 | `invoice_assembly_pipeline._page_num_key` | 🔴 P0 | 读不存在的字段 → 物理连续性判定永远 False → 无页码标记的多页票 100% 拆成 N 张 |
| 2 | `tests/test_invoice_assembly.py::_make_page` | 🔴 P0 | fixture 注入生产不存在的 `page_num` → 测试假绿，掩盖 #1 |
| 3 | `useFileOps.hydrateChunk:758` | 🔴 P0 | 前端消费 assembly 结果时**先按 invoiceNumber 过滤**，违反「实例身份优先」冻结铁律 |
| 4 | `groupDocuments.groupFilesByDocument` | 🔴 P0 | 兜底路径按 `f.docId` 归组，但 docId 已在 hydrate 时被改写为每页独立值 → 兜底也退化为 Page=Invoice |
| 5 | `assembled_documents` 契约 | 🟡 P1 | 后端已知页面归属却不下发，强迫前端用 invoiceNumber 反推（#3 的根因） |
| 6 | `app.py:1228` vs `import_batch_manager:779` | 🟡 P1 | 两个入口用**不同的桶键**（content hash vs instanceId），语义分叉 |
| 7 | `import_batch_manager:514-521` | 🟡 P1 | `total_pages` 嵌套在 `if src_doc_id` 内，与 `instance_id` 写入条件不一致 → 潜在「桶存在但总页数=1」 |
| 8 | `hydrateChunk:755,849` | 🟡 P1 | `fallbackFiles` 声明后从未 push，被 invoiceNumber 过滤掉的页**静默丢失** |
| 9 | `import_batch_manager:517` 注释 | 💭 | 注释称 pageNum 为 0-based，实际 `/split_pdf` 产出 1-based |

---

## 1. 逐阶段模型表

按你要求的四元组格式。

### Stage 1 — PDF 拆页（producer）
`frontend/src/utils/fileHelpers.js:69-131` → `POST /split_pdf`

- **输入模型**：`File`（用户选中的物理 PDF）
- **输出模型**：`FileLike[]`（每页一个）
- **保持 source identity**：✅ 完整
  - `instanceId = crypto.randomUUID()`（:98）——所有页**共享同一个**，正确
  - `sourceDocId = data.doc_id`（父 PDF 内容哈希，:118）
  - `pageNum = page.page_index`（:114）
  - `totalPages`（:119）
- **是否可能产生 invoice**：❌ 否（纯拆分）
- **备注**：`page_index` 实为 **1-based**（`app.py:948` 的 `(i, i+1, ...)` → `:976` 写入 `page_index=page_num`）。命名误导，但后端 `-1` 归一化（`import_batch_manager:769`）与之**匹配**，无 off-by-one 实际缺陷。

### Stage 2 — 传输
`runChunkedImport.js:132-143` → `ImportBatchClient.js:62-73` → `POST /import/batch`

- **输入模型**：`FileLike[]`
- **输出模型**：`pageMetas[]`（四字段与 files 按索引一一对应）
- **保持 source identity**：✅ 四个字段全部透传
- **是否可能产生 invoice**：❌ 否

### Stage 3 — 后端接收 → job.metrics
`import_batch_manager.py:512-534`

- **输入模型**：`pageMetas[]`
- **输出模型**：`Job.metrics{source_doc_id, page_num, total_pages, instance_id}`
- **保持 source identity**：⚠️ **有条件**
- **是否可能产生 invoice**：❌ 否

🟡 **P1 — 写入条件不一致**
```python
if src_doc_id:                    # ← page_num/total_pages 被这个条件圈住
    job.metrics['source_doc_id'] = src_doc_id
    job.metrics['page_num']    = ...
    job.metrics['total_pages'] = ...

instance_id = fi.get('instanceId') or ''   # ← 但 instance_id 是独立的
if instance_id:
    job.metrics['instance_id'] = instance_id
```
**为什么危险**：桶键取自 `instance_id`，完成判据取自 `total_pages`。二者写入条件不同。
一旦出现 `instanceId 有值 / sourceDocId 空` 的组合，`total_pages` 缺失 → `_parse_page_info` 默认回落 **1**（:817）→ 每一页 `put` 立刻返回 `completed=True` → **每页各自 assemble 成一张发票**。
当前多页 PDF 路径下 `sourceDocId` 恒有值，所以没爆——这是巧合，不是设计。

### Stage 4 — PageResultStore
`page_result_store.py`

- **输入模型**：`(bucket_key, page_num, total_pages, PageParseResult)`
- **输出模型**：`PageParseResult[]`（按 page_num 排序）
- **保持 source identity**：✅ `source_doc_id` 作为溯源元数据留存，桶键用 `instance_id`
- **是否可能产生 invoice**：❌ 否
- **评价**：🟢 **这一层是干净的**。职责单一、线程安全、语义边界（Identity vs Capability）在 docstring 里写得很清楚。不是问题源。

🟡 **P1 — 两个入口桶键不一致**

| 入口 | 桶键 | 代码 |
|------|------|------|
| `/import/batch`（主路径） | `instance_id` | `import_batch_manager.py:779` |
| `/parse_invoice`（单文件） | `source_doc_id`（内容哈希） | `app.py:1228` |

`app.py` 路径按内容哈希分桶，与 `page_result_store.py` 自己的 docstring（"IS-4.2：bucket_key = instance_id"）**直接冲突**。后果：同内容 A.pdf / B.pdf 走单文件路径时会落进同一个桶互相污染。

### Stage 5 — InvoiceAssemblyPipeline 🔴 **断点在这里**
`invoice_assembly_pipeline.py:126-208`

- **输入模型**：`PageParseResult[]`（= `parse_invoice_service` 的原始返回）
- **输出模型**：`InvoiceDocument[]`
- **保持 source identity**：N/A（桶内操作）
- **是否可能产生 invoice**：✅ **是——且数量错误**

#### 🔴 P0：`_page_num_key` 读取一个生产中不存在的字段

```python
def _page_num_key(page: Dict) -> int:
    return page.get('page_num') or page.get('page_index') or 0
```

`parse_invoice_service` 的返回值（`services/invoice_service.py:555-575`）完整键集为：
```
file_format, parse_method, invoice_type, invoice_number, amount, invoice_date,
raw_text, raw_text_original, raw_text_for_extract, preview_image, safe_filename,
extra_fields, bbox_data, source_type, file_hash, db_record, db_result,
from_cache, metrics
```
**没有 `page_num`，没有 `page_index`。**

链式后果：
```
_page_num_key(任意页) ≡ 0
   ↓
_physically_consecutive(a, b)  ≡  (0 == 0 + 1)  ≡  False     # 永远
   ↓
分组降级路径（B 分支，:166-173）彻底失效 —— 是死代码
   ↓
分组唯一可用依据 = 页面文本里的「共N页第M页」正则标记
   ↓
无标记 / 标记写法不匹配 → 每页自成一组 → N 张发票
```

#### 运行探针（实测，非推理）

```
输入形态                                    实际分组数    应为
A. 三页同号、无页码标记                          3          1   ❌
B. 三页、续页 invoice_number 为空                3          1   ❌
C. 标记写法「第1页/共2页」「第2页/共2页」          2          1   ❌
D. 标记写法「共2页第1页」「共2页第2页」            1          1   ✅
```

只有 D 一种写法能正确聚合。

C 失败的独立原因：正则第二分支 `第\s*(\d+)\s*页\s*/\s*(\d+)`（:61）要求 `/` 后**紧跟数字**，而全电发票最常见的写法是 `第2页/共2页`——`/` 后是「共」字，匹配失败。这是 #1 之外的**第二个独立缺陷**。

#### 🔴 P0：测试假绿（这是 D-2 "验证通过" 的真相）

`tests/test_invoice_assembly.py:20-41`
```python
def _make_page(invoice_number, page_num, total_pages, ...):
    result = {
        'invoice_number': invoice_number,
        'page_num': page_num,        # ← 生产的 parse 结果里根本没有这个字段
        'total_pages': total_pages,  # ← 同上
        'extra_fields': {'fphm': invoice_number},
    }
```
fixture 喂进了 `page_num`，于是 `_page_num_key` 拿到真实页码，`_physically_consecutive` 正常工作，Case B 三页同票聚合成 1 张 —— **测试通过，生产失效**。

实测：`python tests/test_invoice_assembly.py` → 🎉 全部测试通过。

**这是本次审计最重要的发现。** 测试没有在测生产的数据形状，它在测一个理想化的数据形状。当时 D-2 的"✅ 通过"是一个假信号。

> 建议加一条纪律：**assembly 的测试 fixture 必须由 `parse_invoice_service` 的真实返回构造（或至少用其键集做 schema 断言），禁止手工拼造上游不产出的字段。**

### Stage 6 — DB 写入
`import_batch_manager.py:788-808, 852` / `app.py:1234-1252`

- **输入模型**：`InvoiceDocument[]`
- **输出模型**：DB rows
- **是否可能产生 invoice**：✅ 是（1 InvoiceDocument = 1 row）
- **评价**：这一层**忠实**。它写几条，取决于 Stage 5 给几个。Stage 5 给 3 个，DB 就 3 条。

📌 **回答你的「第一查」**：三页同票 PDF，DB 里会是 **3 条**（除非该票文本恰好含「共3页第N页」写法）。但请注意——**DB 条数是症状，不是根因**。根因在 Stage 5。

⚪ 已排除的嫌疑：`services/invoice_service.py:539` 的直写 `upsert_invoice` 只在 `skip_db_write=False` 时触发；所有生产入口（`app.py:1136`、`ocr_pool_task.py:51`、`import_batch_manager.py:552`、`invoice_parse_coordinator.py:132`）均传 `True`。**你担心的「某个入口绕过 Assembly 直接入库」——不存在。**

### Stage 7 — assembled_documents 契约（后端 → 前端）
`import_batch_manager.py:801-807` → `app.py:1825`

- **输出模型**：
  ```python
  {'instanceId', 'sourceDocId', 'invoiceNumber', 'invoiceType', 'pageCount'}
  ```
- **是否可能产生 invoice**：❌ 否（但决定了前端能否正确还原）

🟡 **P1 — 有损契约（#3 的根因）**

后端在 `assemble()` 里**精确知道**哪几页组成了这张发票，但下发时只给了一个 `pageCount: 3`，把页面成员名单丢了。
前端因此**只能靠 invoiceNumber 反推页面归属** —— 这直接导致 Stage 8 的优先级倒置。

**这是架构层面的信息丢失**：上游已经算出的归属结论，在跨进程边界被降维成一个计数，下游被迫用一个更弱的信号重新推导一遍。

### Stage 8 — hydrateChunk（前端消费） 🔴 **第二个断点**
`frontend/src/hooks/useFileOps.js:753-844`

- **输入模型**：`items[]`（page-level）+ `documents[]`（assembled）
- **输出模型**：`InvoiceDocument`（写入 DocumentStore + session.documents）
- **是否可能产生 invoice**：✅ 是

#### 🔴 P0：分组优先级倒置

```js
for (const assembled of documents) {
  // 第一道闸门 —— invoiceNumber
  const matchingItems = items.filter(i => i.invoiceNumber === assembled.invoiceNumber)   // :758
  const matchingKeys  = new Set(matchingItems.map(i => i.clientKey).filter(Boolean))
  const matchingFiles = readyFiles.filter(f => matchingKeys.has(f.key))

  // 第二道闸门 —— instanceId（在 invoiceNumber 已经筛过之后才生效）
  const { files: sameInstanceFiles } = resolveInstancePageFiles(matchingFiles, assembled) // :777
```

这与冻结铁律**正好相反**：

```
铁律：   实例身份 → pages → assembly → invoice_number
现状：   invoice_number → pages → 实例身份过滤
```

`instancePageOwnership.js` 这个模块本身写得很好（纯函数、fallback 分级、注释解释了为什么用 instanceId 而非 sourceDocId）——**但它拿到的 `matchingFiles` 已经被 invoiceNumber 污染过了**。护栏装在了第二道门上，第一道门是敞开的。

**具体失效场景**（对应你的「情况3：identity 丢失」判断，方向完全正确）：

| 场景 | 结果 |
|------|------|
| 续页 `invoiceNumber` 为空（明细续页常见） | 续页不进 `matchingItems` → Document 只拿到首页 → 续页**静默消失** |
| `assembled.invoiceNumber === ''`（整票未识别号码） | `i.invoiceNumber === ''` 命中**所有**无号页 → 跨发票错误吸收 |
| 两张不同发票同号 | 先被 invoiceNumber 合并，再靠 instanceId 拆回 —— 靠兜底救场，不是靠设计 |

#### 🟡 P1：被过滤掉的页静默丢失

```js
const fallbackFiles = []          // :755  声明
...
for (const fileObj of fallbackFiles) { ... }   // :849  遍历
```
**中间没有任何 `fallbackFiles.push(...)`。** 数组恒为空，:849-872 整段是死代码。

注释（:846-848）说它负责"处理被闸门拒绝的页"，但拒绝路径（:784-787 的 `continue`）只打了一行 `console.warn` 就跳过了，没有把页交给 fallback。
→ **被 invoiceNumber 闸门筛掉的页，既不在 assembled Document 里，也不在 fallback 里，从 session.documents 中彻底消失。**

### Stage 9 — UI ViewModel 🔴 **第三个断点（兜底也坏了）**
`documentViewModel.js:90-94` → `groupDocuments.js` / `invoiceDocumentViewModel.js`

```js
const USE_INVOICE_DOCUMENT_ROWS = Array.isArray(invoiceDocs) && invoiceDocs.length > 0
const documents = USE_INVOICE_DOCUMENT_ROWS
  ? invoiceDocumentsToRows(invoiceDocs, files)   // 主路径
  : groupFilesByDocument(files)                  // 兜底路径
```

- **主路径**：消费 `session.documents`，用 `_pageKeys` 强身份匹配（`invoiceDocumentViewModel.js:47-55`）。设计正确 ✅ —— 但它吃的是 Stage 8 的产物，Stage 8 错了它就跟着错。
- **兜底路径** 🔴 **独立损坏**：

```js
if (!f?.docId) continue
let instances = docInstances.get(f.docId)     // groupDocuments.js:70 —— 按 docId 归组
```

模块头部注释（:18-22）明确假设 *"docId 是内容哈希 …… 合法多页文档的 pageNum(1..N) 各出现一次"*，即**同一份多页 PDF 的所有页共享父 PDF 的 docId**。

但 `hydrateChunk:720-724` 把这个前提拆了：
```js
if (item?.docId && fileObj.docId !== item.docId) {
  const enriched = updateDocumentIdentity(fileObj, item.docId)
  fileObj.docId = enriched.docId        // ← 改写为「每页自己的物理 docId」
```
拆出来的每一页是独立的物理 PDF，各有各的 sha256 → hydrate 后**每页 docId 互不相同** → `groupFilesByDocument` 每页一组 → **兜底路径同样退化为 Page = Invoice**。

📌 **回答你的「第三查」**：`ImportSessionStore` / `DocumentStore` 的键策略（`instanceId || docId || id`）本身是对的，两个 store 的 `resolveDocumentIdentity` 也对齐了。**问题不在 store，在 `groupDocuments` 的 docId 语义假设已经过期。**

---

## 2. 一张图

```
File
 │
 ├─ Stage1 split ────── instanceId ✅ sourceDocId ✅ pageNum ✅ totalPages ✅
 ├─ Stage2 transport ── 四字段全透传 ✅
 ├─ Stage3 metrics ──── ⚠️ total_pages 与 instance_id 写入条件不一致
 ├─ Stage4 PageStore ── ✅ 干净（唯一无瑕疵的一层）
 │
 ├─ Stage5 Assembly ─── 🔴 读 page_num（生产不存在）
 │                       → 物理连续性判定死掉
 │                       → 只剩正则标记，且正则漏了「第N页/共M页」
 │                       → N 页 = N 票                        ← 断点 ①
 │
 ├─ Stage6 DB ───────── 忠实执行上游结论（写 3 条不是它的错）
 ├─ Stage7 契约 ─────── 🟡 丢掉页面成员名单，只给 pageCount
 │
 ├─ Stage8 hydrate ──── 🔴 先 invoiceNumber 后 instanceId（优先级倒置）
 │                       + fallbackFiles 死代码 → 页静默丢失   ← 断点 ②
 │
 └─ Stage9 UI ───────── 主路径依赖 Stage8
                        兜底 groupFilesByDocument 按 docId 归组，
                        而 docId 已被改写为每页独立                ← 断点 ③
```

**三个断点相互独立**，任何一个单独存在都足以让 UI 显示 N 张发票。这就是为什么「看起来像旧逻辑复活」——实际是三处各自的契约假设同时过期。

---

## 3. 关于「项目详细不全」

你的判断准确。当前代码里**同时存在三套 Document 语义**，且没有一处显式声明谁是权威：

| 语义 | 载体 | 键 | 谁在用 |
|------|------|-----|--------|
| Physical Document | `docId`（内容哈希） | sha256[:24] | 渲染 / 预览 / 缓存 |
| File Instance | `instanceId`（UUID） | randomUUID | 分桶 / Page Ownership |
| Invoice Document | `invDocId` | `${fileKey}_inv_${invoiceNumber}` | UI 行 / 导出 |

`hydrateChunk:799-803` 里三者在**同一个函数体内**被同时构造和消费，靠注释区分。这是它难读、也是最容易再次退化的原因。

与已冻结的 P8（Identity ≠ Capability）是**同构问题的第三个实例**：低层标识被当作高层语义使用。建议归入同一条演进线，不要各修各的。

---

## 4. 建议的修复顺序（本次不执行）

严格按依赖排序，每步一个 commit、一次验证（遵循 bisect 纪律）：

1. **先修测试，再修代码**
   把 `_make_page` 换成由 `parse_invoice_service` 真实键集构造的 fixture，或加一条 schema 断言。
   → 预期 Case B 立刻变红。**红了才说明测试开始有价值。**

2. **修 Stage 5 数据流**（不改分组算法）
   让 `page_num` 真正进入 `PageParseResult`——在 `store.put` 时注入，或让 assemble 接收 `(page_num, result)` 元组。
   → 物理连续性降级路径复活，探针 A/B 应变 1 组。

3. **修页码正则**
   补 `第(\d+)页\s*/\s*共(\d+)页` 分支。
   → 探针 C 应变 1 组。

4. **扩 `assembled_documents` 契约**
   下发 `pageClientKeys: [...]`（后端本来就知道）。

5. **改 Stage 8 优先级**
   先按 `instanceId` 圈定候选，再按 assembly 下发的页名单取页；`invoiceNumber` 降级为**校验**而非**筛选**。
   顺便处理 `fallbackFiles` 死代码——要么填充它，要么删掉并让拒绝路径显式告警。

6. **修 Stage 9 兜底**
   `groupFilesByDocument` 改按 `instanceId` 归组（或在 hydrate 时保留 `parentDocId` 字段供其使用），并更新那段已过期的头部注释。

7. **统一桶键**
   `app.py:1228` 从 `source_doc_id` 改为 `instance_id`，与 `page_result_store` 的 docstring 对齐。

**建议的验收探针**（每步跑一次，30 秒）：
```bash
cd backend && python -c "..." # 本报告 §Stage5 的 A/B/C/D 四形态
```

---

## 5. 值得肯定的部分

审计不是只挑错。这套代码里有几处做得确实好：

- **`page_result_store.py`** — 职责边界干净，docstring 把「为什么用 instance_id 而非内容哈希」讲清楚了。这是整条链路里唯一没有瑕疵的一层。
- **`instancePageOwnership.js`** — 纯函数、fallback 分三级并把告警交给调用方（便于 Node 单测），设计意图明确。只是被放在了错误的位置。
- **注释里的「为什么」密度很高** —— `useFileOps.js:768-775` 那段模型边界说明，让这次审计快了至少一半。多数项目做不到这点。

问题不在代码质量，在**跨层契约缺少机器可验证的约束**。三个断点全都是「A 层假设 B 层会给某个字段，B 层从来没给过，而测试替 B 层给了」。

---

*审计人：Code Reviewer ｜ 未修改任何生产代码*

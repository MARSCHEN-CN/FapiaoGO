# 前端重复导入提醒 — 链路调研与最小接线点（只读，不改码）

> 阶段：InvoiceImportHistory 后端已冻结完成；本调研只定位前端真实入口，不改动任何代码。
> 目标：钉死「解析完成拿到 `invoiceNumber`」的节点 + 现有「导入确认 UI」的真实落点，给出最小接线点。

---

## 1. 结论先行

- **解析完成节点已钉死**：file 对象在 `status === 'parsed'` 后，`file.invoiceNumber` / `file.invoiceDate` 即可用（与现有 `getPreviousYearInfo` 读取同源）。
- **现有「导入确认 UI」= 软警告体系（往年发票 / 重复组）**，非阻塞、带「移除 / 关闭提示」操作。当前**没有提交前硬确认弹窗**——导入在拖入后即进入 `split → parse → build → 后端 upsert` 流水线。
- **后端 API 已就绪**：`GET /api/import-history/<number>`（`backend/app.py` 已加）。前端 client 需新增 `getImportHistory(number)` 镜像 `findDuplicates`。
- **最小接线点**：复用 previousYear / duplicate 的 advisory（提醒不拦截）模式，新增 `importHistoryInfo` 这个 `Map<file.key, hit>`，非阻塞展示 + 可选移除。

---

## 2. 真实入口钉死（文件:行号，均只读确认）

### A. 解析完成、`invoiceNumber` 可用
- `frontend/src/utils.js:321-322` —— `getPreviousYearInfo` 读取 `f.invoiceDate`；同期的发票号码字段为 `file.invoiceNumber`（见 `utils.js:228`）。
- 即文件对象在 `status === 'parsed'` 后已携带 `invoiceNumber` / `invoiceDate`。**这是预查询 ImportHistory 的数据可用点。**
- 解析+导入流水线：`frontend/src/hooks/useFileOps.js:311 processFilesForAddition` → `:651 await runChunkedImport(...)` → `frontend/src/import/runChunkedImport.js:63`（split→parse→build 纯编排层）。

### B. 导入「提交」节点（后端挂钩处）
- `runChunkedImport` 的 build 步调用后端 `batch_upsert_invoices` → 我们后端的 `record_import` 挂钩在此触发（`backend/db.py`）。
- 即「提交」= 流水线内的后端 upsert，**无独立 UI 确认按钮**。这与「拖入即导入」模型一致。

### C. 现有「导入确认 / 软警告」UI（对标落点）
- **模式1 往年发票**（最贴近我们要做的）：
  - `frontend/src/utils.js:314 getPreviousYearInfo` 产出 `Map<file.key, {year, isPreviousYear}>`；
  - `frontend/src/contexts/FileContext.jsx:128` 在 `useMemo` 内计算 `previousYearInfo` + `documentView.previousYearCount`；
  - `frontend/src/components/Sidebar.jsx:435-464` 警告块（计数 + 「移除往年」+ 「关闭提示（视为正常状况）」）；
  - `frontend/src/components/FileList.jsx:24-25` 行级 badge `isPrevYear`。
- **模式2 重复组**：`duplicateGroups` / `duplicateDocumentInfo`（`App.jsx` / `FileContext.jsx` / `FileList.jsx:18-19 isDuplicate`），同样 advisory 结构。
- **告警优先级**：`frontend/src/utils.js:334 FILE_ALERT_PRIORITY = ['failed','duplicate','previousYear']`（可安全追加 `'importHistory'`）。
- **「提醒不拦截」既有证据**：`Sidebar.jsx:459` 「关闭提示（视为正常状况）」——这正是冻结语义在当前 UX 里的落点。

### D. 后端 API client 模式（供新增镜像）
- `frontend/src/db.js:124 findDuplicates(number)` → `apiGet('/api/db/duplicates/' + encodeURIComponent(number))`。
- 新增 `getImportHistory(number)` 与之**完全同构**（仅需确认后端 `/api/import-history/<number>` 的返回字段与前端命名对齐）。

---

## 3. 最小接线点建议（下一阶段实施，本阶段不动）

### 方案甲（推荐：最小、保语义）
1. `frontend/src/db.js` 新增 `getImportHistory(number)`（镜像 `findDuplicates`）。
2. 在 `FileContext` 内用 effect / 新增 util：对 `status==='parsed'` 且 `invoiceNumber` 非空的 file，调 `getImportHistory`（**防抖 / 批量**），产出
   `Map<file.key, {exists, invoiceDate, firstImportedAt, importCount, dateMismatchCount}>`。
3. 暴露 `importHistoryInfo` + document 级计数到 context（对齐 `previousYearCount`）。
4. 展示（平行新增，不改既有逻辑）：
   - `Sidebar.jsx` 计数 + 警告块（「X 个曾导入发票」+ 「移除」/「关闭提示」）；
   - `FileList.jsx` 行级 badge（镜像 `isPrevYear`）。
5. 移除动作镜像 `removePreviousYearFiles`（`Sidebar.jsx:574` / `App.jsx:983`）。
- **关键红线**：全程 advisory，**绝不阻断** `processFilesForAddition` / `runChunkedImport`。用户「继续」= 正常提交，后端 `record_import` 仍按「仍然导入」累加（`importCount+1`）——与冻结语义一致。

### 方案乙（较大，可选，不推荐作最小接线点）
- 在 `runChunkedImport` 的 parse 与 build 之间插入显式确认闸门（弹窗列明曾导入项，`[取消]/[仍然导入]`）。
- 改动大、需小心保持「不拦截」语义，且与现有「拖入即导入」模型冲突，留作备选。

---

## 4. 风险与注意（实施前必读）

- **异步取数并发**：ImportHistory 需按号码调 API，必须防抖 / 并发限制，避免一次拖入 100 张打 100 个请求。可借鉴 `subscribeBatchProgress` 的批量思路，或后端加批量接口 `GET /api/import-history/batch`。
- **号码归一化一致性**：前端查询参数应与后端 `normalize_invoice_number`（strip + 去内部空白 + 大写）保持一致，否则可能查不到历史。
- **不阻塞红线**：无论甲 / 乙，禁止把「曾导入」变成硬拦截（与冻结约束冲突）。

---

## 5. 当前未决（待你拍板后再进下一阶段）

1. 采用**方案甲**（advisory，贴合现有往年 / 重复模式）还是**方案乙**（显式确认闸门）？
2. 方案甲若需支撑高并发，是否要后端补一个批量查询接口 `/api/import-history/batch`？

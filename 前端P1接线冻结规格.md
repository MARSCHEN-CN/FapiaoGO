# 前端 P1 接线：ImportHistory Advisory（冻结规格 2026-08-17）

> 本文件是 **InvoiceImportHistory 子系统** 的前端冻结规格。后端（`backend/import_history.py` + `GET /api/import-history/<number>`）已冻结完成；本文件只规定前端如何接线，**不改动后端、不改变既有产品模型**。

## 0. 已拍板决策

| 决策 | 内容 |
| --- | --- |
| 接线方案 | **方案甲：advisory 平行接入**（复用现有 previousYear / duplicate 软警告体系） |
| Batch API | **暂不新增** `GET /api/import-history/batch`；并发控制在**前端**做，后端保持 O(1) 内存查 |
| 数据挂载 | `importHistoryInfo` 只存 `Map<file.key, hit>`，**不挂在 file 实例上**、**不修改** `file.invoiceNumber` / `file.invoiceDate` |
| 业务语义 | **提醒不拦截**：命中历史只作风险提示，用户可移除或照常进入原有导入流水线 |
| 后端边界 | **不修改** `backend/import_history.py` / `db.py` 挂钩 / `app.py` 接口 |

## 1. 数据流（冻结）

```text
file.status === 'parsed'
        │  file.invoiceNumber 已可用
        ▼
  查询 ImportHistory（GET /api/import-history/<number>）
        │
        ▼
  命中？
   ├─ 否 → 无任何 UI 变化
   └─ 是 → Sidebar / FileList 风险提醒（行级 badge + 汇总警告块）
              │
              ▼
          用户可「移除」该文件（复用现有移除机制）
              │
              ▼
          否则照常进入原有 runChunkedImport 导入流水线（语义不变）
```

## 2. 真实接线点（来自只读调研，未改码）

| # | 位置 | 动作 |
| --- | --- | --- |
| 1 | `frontend/src/db.js`（镜像 `findDuplicates` @ `db.js:124`） | 新增 `getImportHistory(number)`：调 `GET /api/import-history/<number>`，返回 `{exists, invoiceDate, firstImportedAt, lastImportedAt, importCount, dateMismatchCount}` |
| 2 | `frontend/src/context/FileContext.jsx` | 新增 state `importHistoryInfo`（见 §3）；`parsed` 后防抖触发查询 |
| 3 | `frontend/src/components/FileList.jsx`（平行 `FileList.jsx:24` 行级 badge） | 命中时在对应行渲染风险提示 badge |
| 4 | `frontend/src/components/Sidebar.jsx`（平行 `Sidebar.jsx:435` 警告块） | 汇总提示：命中数量 + 警告文案，复用现有「关闭提示（视为正常状况）」非阻塞交互 |
| 5 | 现有「移除文件」动作 | **复用**，不新增确认闸门 |

## 3. `importHistoryInfo` 结构（冻结）

```js
// FileContext state
const [importHistoryInfo, setImportHistoryInfo] = useState(
  new Map() // Map<file.key, Hit>
);

// Hit 结构（与后端 GET 响应 1:1，不扩展、不嵌套到 file）
{
  exists:           boolean,
  invoiceDate:      string,   // "2026-08-01"
  firstImportedAt:  string,   // ISO datetime
  lastImportedAt:   string,   // ISO datetime
  importCount:      number,
  dateMismatchCount:number
}
```

**红线**：`importHistoryInfo` 是 file 对象之外的**纯旁路风险信息层**；`File.invoiceNumber` / `File.invoiceDate` 只读，接线过程不得写入或覆盖这两个字段。

```text
File
 ├─ invoiceNumber   (解析层写入，本接线只读)
 ├─ invoiceDate     (解析层写入，本接线只读)
 └─ ...

ImportHistoryInfo   (本接线写入，旁路层)
 └─ file.key → Hit
```

## 4. 前端并发控制（冻结，替代 batch API）

- **范围**：仅对 `status === 'parsed' && file.invoiceNumber` 的 file 发起查询。
- **同号去重**：先用归一化号码建 `Map<normalizedNumber, [fileKeys]>`，一个号码只发 1 个请求，结果映射回所有同号文件（避免同号文件重复请求）。
- **并发上限**：4～8 个在途请求（p-limit / 自建信号量）。
- **失败降级**：单个查询失败 → **静默忽略**（该 file 不出现在 `importHistoryInfo`），**绝不阻断**正常导入流水线。
- **触发时机**：file 进入 `parsed` 后防抖（如 300ms）批量发起；避免在 `runChunkedImport` 内部插入任何同步等待。

> 若后续实测 500～1000 张批量导入时 HTTP 往返成为瓶颈，再评估引入 `GET /api/import-history/batch`；**当前不提前扩大后端 API 面**。

## 5. 不改动红线（冻结）

1. ❌ 不修改 `runChunkedImport` / `useFileOps.processFilesForAddition` 的**提交语义**（保持「拖入即导入」）。
2. ❌ 不增加任何「导入前确认闸门」/ 硬拦截弹窗。
3. ❌ 不增加 `batch` API（见 §4）。
4. ❌ 不修改后端 `InvoiceImportHistory`（`import_history.py` / `db.py` 挂钩 / `app.py` 接口）。
5. ❌ 不修改 `file.invoiceNumber` / `file.invoiceDate`。

## 6. 边界总结

```text
后端   = 历史事实（InvoiceImportHistory，已冻结）
前端   = 风险呈现（ImportHistory Advisory，本规格）
       
两者通过 GET /api/import-history/<number> 单接口解耦；
前端永不直接读写 invoice_import_history.json。
```

## 7. 下一阶段流程

> 只读实现前审查 → 最小代码修改（仅 §2 的 5 个接入点）→ 回归验收

验收要点：
- 命中历史 → Sidebar 汇总 + FileList 行级提示均出现；
- 移除文件 → 提示消失，不影响其他文件；
- 不移除 → 文件照常导入（后端 `record_import` 仍按规则更新历史，语义不变）；
- 查询失败（断网/接口异常）→ 静默降级，导入照常；
- `file.invoiceNumber` / `invoiceDate` 在导入前后未被本接线改写（可加断言/快照比对）。

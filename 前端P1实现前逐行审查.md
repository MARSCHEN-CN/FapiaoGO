# 前端 P1 接线：实现前逐行审查（只读，2026-08-17）

> 本文件是对 `前端P1接线冻结规格.md` 的**代码级审查**。已逐行读取真实源码（本工作区可访问），把 5 个接入点的精确行号、现状片段、以及你提出的 4 个竞态对应的代码事实全部坐实。**本阶段不改任何代码。**

## 0. 总体结论

设计成立，**可以进入最小实现**。后端 `GET /api/import-history/<number>` 已就绪，前端 5 个接入点均有明确、低风险的插入位。4 个竞态均可在现有代码模式内化解，无需引入新基础设施。

---

## 1. 逐文件接入点（精确行号 + 现状 + 建议）

### 1.1 `frontend/src/db.js`（镜像 findDuplicates）

**现状**（db.js:123-126）：
```js
/** 去重检查 */
findDuplicates(number) {
  return apiGet(`/api/db/duplicates/${encodeURIComponent(number)}`)
},
```
**建议**：在 `findDuplicates` 之后新增同形方法：
```js
/** 发票导入历史（防重复报销软提醒） */
getImportHistory(number) {
  return apiGet(`/api/import-history/${encodeURIComponent(number)}`)
},
```
`apiGet` 已是本项目 GET 封装（findDuplicates 同款），无需新依赖。

---

### 1.2 `frontend/src/contexts/FileContext.jsx`（查询协调层 + 旁路 state）

**现状关键位**：
- `files` 来自 `useReducer`（`INITIAL_STATE = { files: [] }`，line 23），`setFiles` 为 `useCallback`（line 35-39）。
- 已有 **带签名的派生缓存** `derivedCache` ref（line 106），以及 `useMemo`（line 108-134）按 `combinedSig` 仅在查询集合变化时重算 `previousYearInfo` 等——这是 race #3（Stable signature 防 StrictMode 重复查询）的**现成范式**。
- 上下文 `value` 已暴露 `previousYearInfo / duplicatePageInfo / duplicateDocumentInfo`（line 189-204）。

**建议插入**：
1. 新增 state：`const [importHistoryInfo, setImportHistoryInfo] = useState(() => new Map())`（旁路 Map<file.key, hit>，不挂 file）。
2. 新增 3 个 ref：`importHistorySigRef`（已发查询签名）、`importHistoryReqRef`（请求令牌，防过期回写）、`importHistoryTimerRef`（debounce 句柄）。
3. 新增一个 **fire-and-forget 异步 effect**（依赖 `[files]`），内部用签名比对 + 令牌 + validKeys 快照化解 4 竞态（见 §2 骨架）。
4. 在 `value`（line 176-204）中暴露 `importHistoryInfo`。

> **红线守住**：该 effect 是独立副作用，**不进入** `runChunkedImport` / `useFileOps` 的调用链，也不被其 `await`（race #4）。`db.getImportHistory` 的调用体必须 `try/catch`，失败静默降级。

---

### 1.3 `frontend/src/components/Sidebar.jsx`（汇总警告分支 + 透传 prop）

**现状关键位**：
- line 64：`const { files, ..., previousYearInfo, duplicateDocumentInfo } = useFileContext()` —— 从 context 取派生数据。
- line 99-100：`previousYearCount = documentView.previousYearCount; hasPreviousYear = previousYearCount > 0`。
- line 369-380：`<FileList ... previousYearInfo={previousYearInfo} ... />` —— 透传给列表。
- line 435-464：统计区的 `hasPreviousYear && showPreviousYearWarning` 警告分支（含「移除往年」按钮 + 「关闭提示（视为正常状况）」非阻塞关闭）。

**建议插入**：
1. line 64 解构补 `importHistoryInfo`。
2. 在 line 100 后新增：`const importHistoryCount = useMemo(() => { let c=0; importHistoryInfo.forEach(h => { if (h?.exists) c++ }); return c }, [importHistoryInfo]); const hasImportHistory = importHistoryCount > 0`（注意：因历史是**异步**结果，计数须来自 `importHistoryInfo` state，而非 `documentView`）。
3. line 369-380 的 `<FileList>` 透传 `importHistoryInfo={importHistoryInfo}`。
4. 在 line 435 的条件链中**追加一个平行分支**（建议置于 previousYear 之后、normal 之前，作为最低优先级 advisory）：`hasImportHistory && showImportHistoryWarning ? (…移除历史命中…) :`。该分支复用「关闭提示（视为正常状况）」的非阻塞关闭交互（与 previousYear 同构），**不新增确认闸门**。
5. 新增 `const [showImportHistoryWarning, setShowImportHistoryWarning] = useState(true)`（line 62-63 旁）。

---

### 1.4 `frontend/src/components/FileList.jsx`（行级 badge）

**现状关键位**：
- line 10：`FileCardRow` 已接收 `previousYearInfo` 为 prop。
- line 24-31：`const prevInfo = previousYearInfo?.get(fileObj.key); const isPrevYear = !!prevInfo?.isPreviousYear;` 以及 `showPrevYear` 优先级链（`!hasFailed && !showDuplicate && isPrevYear`）。
- line 85：className 含 `${showPrevYear ? 'previous-year' : ''}`。
- line 89+：往年 badge 渲染块。

**建议插入**：
1. line 10 的 `FileCardRow` props 补 `importHistoryInfo`。
2. line 24 旁：`const histInfo = importHistoryInfo?.get(fileObj.key); const showImportHistory = !!histInfo?.exists;`
3. 优先级链 line 31 后追加一层：`const showImportHistorySafe = !hasFailed && !showDuplicate && !showPrevYear && showImportHistory;`（保持「失败 > 重复 > 往年 > 历史命中」的既有优先级语义，沿用 `FILE_ALERT_PRIORITY` 思路）。
4. line 85 className 补 `${showImportHistorySafe ? 'import-history' : ''}`。
5. line 89 的往年 badge 块之后，平行新增历史命中 badge（文案示例：「曾导入·风险提醒」，展示 `firstImportedAt`/`importCount`）。

---

### 1.5 `frontend/src/App.jsx`（复用移除机制）

**现状关键位**：
- line 574-622+：`removePreviousYearFiles(removeSource)` —— 先 `getPreviousYearInfo(liveFiles)` 取 key 集合，再走「`delete-files` IPC 物理删源 + `deleteInvoiceDocument` 删装配条目 + 预览清理」通用移除流程。
- line 982-983：把 `removePreviousYearFiles` / `removeDuplicateFiles` 作为 prop 传给 `<Sidebar>`。
- line 58：`Sidebar` 已声明接收 `removePreviousYearFiles` / `removeDuplicateFiles`。

**建议插入**：
1. 在 `removePreviousYearFiles` 之后新增 `removeImportHistoryFiles(removeSource)`：**几乎完全镜像**，唯一差异是把 key 来源从 `getPreviousYearInfo(liveFiles)` 换成遍历 `importHistoryInfo` state 取 `hit.exists` 的 key 集合；其余「删源文件 IPC / `deleteInvoiceDocument` / 预览清理」流程原样复用（这就是「复用现有移除机制」，不重造）。
2. line 982-983 旁把 `removeImportHistoryFiles` 透传给 `<Sidebar>`；line 58 解构补接收。
3. `Sidebar` line 443 的「移除往年」按钮处，平行新增「移除历史命中」按钮调 `removeImportHistoryFiles(removeSourceFile)`。

---

## 2. 4 个竞态 → 代码级落点（含 StrictMode 实测事实）

> **关键事实**：`main.jsx:8` `<StrictMode>` **已启用**；`App.jsx:806-815` 注释明确记录 React 18 StrictMode 双调用已造成过实际 bug。因此 race #3 不是假设，必须按项目既有范式处理。

| 竞态 | 代码级化解方案 | 落点 |
| --- | --- | --- |
| **#1 结果晚于文件生命周期** | 发起时快照 `validKeys = new Set(files.map(f=>f.key))`；响应回写前 `if (!validKeys.has(k)) continue`；并配 `reqIdRef` 令牌过期检查 | FileContext effect |
| **#2 同号多 file → 1 请求** | 建 `Map<normalizedNumber, fileKey[]>`；按 normalizedNumber 循环发起，结果广播到所有 fileKey | FileContext effect 查询构建期 |
| **#3 StrictMode 重复查询** | ①`reqIdRef` 令牌：每次 effect 运行 `++`，响应时 `if (reqId !== reqIdRef.current) return` 丢弃过期结果；②签名 `firedSigRef`：仅在真正发起时写入，**cleanup 中重置为空**，避免双挂载留下 stale ref 阻断 remount 的 fetch；③`if (sig === firedSigRef.current) return` 跳过无变化重查 | FileContext effect |
| **#4 查询不能成为 pipeline 依赖** | effect 完全独立、fire-and-forget；`runChunkedImport`/`useFileOps` 零改动；不在任何导入路径 `await` 历史查询 | 架构红线（已保证） |

### ⚠️ 实现时必须注意的 StrictMode 细节（审查发现）

若采用「`importHistorySigRef` 早返回 + 不在 cleanup 重置」的朴素写法，**在 StrictMode 下会静默失效**：
```
mount1: sig != ref(空) → 写 ref=sig，setTimeout(timer1)
unmount1(cleanup): clearTimeout(timer1)   // ref 未重置
remount2: sig === ref → 早返回，不再设 timer → 永不查询
```
**正确做法**：`firedSigRef` 在 cleanup 中重置为 `''`，使 remount 重新发起；过期回写由 `reqIdRef` 令牌兜底丢弃。此细节必须写进实现，否则 dev 下历史提醒永不出现、却能通过 prod 构建（StrictMode 仅 dev 双调用）——这是最隐蔽的回归点。

---

## 3. 号码归一化（审查发现的新事实）

**前端当前没有 `normalizeInvoiceNumber` helper**（grep `utils/` 仅见后端语义的引用，无前端实现）。后端 `import_history.py` 已对 key 做归一化。

- **影响**：race #2「同号只请求一次」若直接用原始 `invoiceNumber`，则 `A001` 与 `a001` 会被视为两个号码 → 两次请求（功能无损，但不满足「同号去重」的精确语义）。
- **建议**：在 `FileContext.jsx` **模块内**定义一个极小 `normalizeInvoiceNumber(n)`（trim + 去空白 + 转大写，与后端 `normalize_invoice_number` 对齐）。**不引入 `utils.js` 改动**，保持改动面严格在冻结的 5 文件内。

---

## 4. 红线复核（不改项 ✅ 已确认）

| 红线 | 代码事实 |
| --- | --- |
| 不改 `runChunkedImport.js` | 查询 effect 在 FileContext，与之零交集 ✅ |
| 不改 `useFileOps.js` 提交语义 | 不触碰 processFilesForAddition/runChunkedImport ✅ |
| 不改 `file.invoiceNumber` / `file.invoiceDate` | `importHistoryInfo` 是独立 Map，仅 `get(file.key)` 读取，从不写 file ✅ |
| 不增 batch API | 并发控制在 FileContext effect（4~8），后端不变 ✅ |
| 不增确认闸门/硬拦截 | Sidebar 分支复用「关闭提示=视为正常」，移除走既有机制 ✅ |
| 不改后端 | 仅消费既有 `GET /api/import-history/<number>` ✅ |

---

## 5. 推荐最小 effect 骨架（只读伪码，尚未应用）

```jsx
// FileContext.jsx（模块内）
const normalizeInvoiceNumber = (n) => (n || '').toString().trim().replace(/\s+/g, '').toUpperCase()

// state / refs
const [importHistoryInfo, setImportHistoryInfo] = useState(() => new Map())
const firedSigRef = useRef('')
const reqIdRef = useRef(0)
const timerRef = useRef(null)

useEffect(() => {
  const parsed = files.filter(f => f.status === 'parsed' && f.invoiceNumber)
  const byNumber = new Map()
  for (const f of parsed) {
    const n = normalizeInvoiceNumber(f.invoiceNumber)
    if (!n) continue
    if (!byNumber.has(n)) byNumber.set(n, [])
    byNumber.get(n).push(f.key)
  }
  const sig = [...byNumber.entries()]
    .map(([n, ks]) => `${n}:${[...ks].sort().join(',')}`).sort().join('‖')
  if (sig === firedSigRef.current) return          // race #3: 查询集未变不重查
  firedSigRef.current = sig
  if (byNumber.size === 0) { setImportHistoryInfo(new Map()); return }

  const reqId = ++reqIdRef.current
  if (timerRef.current) clearTimeout(timerRef.current)
  timerRef.current = setTimeout(() => {
    const validKeys = new Set(parsed.map(f => f.key))   // race #1: 有效 key 快照
    const out = new Map()
    const items = [...byNumber.entries()]
    const CONC = 6
    ;(async () => {
      for (let i = 0; i < items.length; i += CONC) {
        if (reqId !== reqIdRef.current) return          // race #1/#3: 已过期
        const batch = items.slice(i, i + CONC)
        const settled = await Promise.allSettled(
          batch.map(async ([n, ks]) => {
            try { return await db.getImportHistory(n) } catch { return null }  // 失败静默
          })
        )
        if (reqId !== reqIdRef.current) return
        settled.forEach((s, idx) => {
          const r = s.status === 'fulfilled' ? s.value : null
          if (!r || !r.exists) return
          for (const k of items[i + idx][1]) {
            if (!validKeys.has(k)) continue             // race #1: 文件已移除不回写
            out.set(k, r)
          }
        })
      }
      if (reqId !== reqIdRef.current) return
      setImportHistoryInfo(out)
    })()
  }, 300)

  return () => {                                        // cleanup：重置签名，放行 remount 重新发起
    if (timerRef.current) clearTimeout(timerRef.current)
    firedSigRef.current = ''
  }
}, [files])
```

> 该骨架为审查建议，**未写入源码**。实现时按此落地，并配套 §6 验收。

---

## 6. 验收清单（含你新增的红线测试）

1. ✅ 命中历史的文件 → Sidebar 汇总 + FileList 行级 badge 均出现。
2. ✅ 移除该文件 → 提示消失，不影响其他文件（`deleteInvoiceDocument`/`delete-files` 既有流程）。
3. ✅ 不移除 → 文件照常导入（后端 `record_import` 仍按规则更新历史，语义不变）。
4. ✅ 查询失败（断网/接口异常）→ 静默降级，导入照常、无报错。
5. ✅ `file.invoiceNumber` / `invoiceDate` 在导入前后未被本接线改写（可加快照断言）。
6. 🔴 **新增（你拍板的红线）**：`history` 查询**永远不能增加 `runChunkedImport` 的等待时间**——可在 e2e 中测量「`status==='parsed'` 到「导入流水线启动」的间隔」，确认与未接 history 查询时一致（查询 effect 是并行旁路，不阻塞）。
7. 🔴 **StrictMode 专项**：dev 下（双调用）首次加载即能出现历史提醒（验证 §2 的 cleanup 重置签名细节，避免「remount 后永不查询」静默失效）。

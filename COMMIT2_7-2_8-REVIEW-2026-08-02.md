# Commit 2.7 + 2.8 评审与验收（backend WIP 分诊后拆分）

> 路线：冻结现场 → 四文件 `git diff HEAD` 分诊 → 确认单一主题、跨两边界 → 拆 2.7(领域层) + 2.8(导出格式化) → 不合并 → 先 push 再进 Commit 4。

## ✅ 结果总览

| Commit | Hash | 范围 | 边界 |
|---|---|---|---|
| **2.7 amount 真源统一** | `47135be0` | multi_page_merge / import_batch_manager / invoice_assembly_pipeline / app.py(响应同步块) + 测试 | 领域层 amount 来源 |
| **2.8 导出金额归一化** | `2936db98` | app.py `_clean_amount_str`（+`$`） | Export Adapter 格式化 |

两 commit 均已 push `origin/master`（`3294adef..47135be0..2936db98`）。

---

## 🔍 分诊结论（冻结现场后）

四个 backend WIP 文件**表面是一个用户问题（amount 不一致），实跨两个架构边界**：

```
InvoiceDocument / Assembly / DTO / Response   ── 2.7 ──► 业务金额真源
Export Adapter                             ── 2.8 ──► 展示格式清洗
```

若合成一个 commit，未来无法二分「金额为什么变」是业务换源还是导出格式化。故拆。

**`multi_page_merge.py` 两层风险解除**：`git diff HEAD` 不含 `line_items_excel_rows`
（Commit 2.5 已提交、完好），工作树 diff 仅为 amountHj 改动 + `first_ef/last_ef` 提取上移。
经核实 2.5 内容完整、无重叠冲突。

---

## 🧩 hunk 拆分（Commit 2.7/2.8 边界关键）

`app.py` 含两个独立 hunk：
- hunk1 `@@ -87` `_clean_amount_str` 定义 + `_build_export_header` 用法 → **Commit 2.8**
- hunk2 `@@ -1248` `parse_invoice()` 响应同步块 → **Commit 2.7**

用 `git add -p` 对 app.py 输入 `n`（hunk1）/ `y`（hunk2）精确拆分：
- Commit 2.7 暂存后 `git diff --cached backend/app.py | grep '^@@'` → **仅 1 个 hunk（1248）**
- `git diff backend/app.py`（未暂存）仍含 `_clean_amount_str` → 留待 2.8
- Commit 2.8 提交后 `git diff backend/app.py | grep '关键修复'` → 0（无响应块泄漏）

---

## 📜 金额真源规则更新（领域事实）

```
旧：multi-page amount = last page amount
新：multi-page amount = last page extra_fields.amountHj
                     fallback last page amount
即 amount = last.extra_fields.amountHj or last.amount
```

`amount` = OCR 初始结果；`amountHj` = AmountExtractor 校验过的真价税合计。
三处领域层统一收口：merge 结果 / DTO / DB 记录；响应层回填 `result['amount']=merged_ef.amountHj`。

---

## 🧪 测试与假绿证伪

`backend/tests/test_commit2_assembled_contract.py`（gitignored，本次 `-f` 强制纳入）：
- 原有 5 例（末页金额 300≠1300 / 首页日期 / 单页 / page_client_keys ×2）不回归
- **新增 `test_amount_hj_preferred_over_raw_amount`**：
  fixture `page1=1000/1100, page2=300/350` → `assembled.amount == 350`（非 300/1300/1450）
- **假绿证伪**：临时改回 `merged['amount'] = last.get('amount')` → 该例 FAIL `300 != 350` → 还原后 PASS。确属真回归锁。
- commit2 套件 **6/6 绿**

---

## 📋 提交纪律核对

- ✅ 未 reset / 未 checkout 任何文件（按裁决冻结）
- ✅ backend 4 文件全部分诊并提交，push 后 `git status backend/` **为空**
- ✅ 3 个预存在前端 WIP（Sidebar / PrintConfirmModal / previewResourceResolver）**未触碰**
- ✅ 未顺手清理其它修改；未跟踪文档/`.build-verify/` 等 Artifact 未纳入

---

## ➡️ 移交 Commit 4

backend 基线现已**无未提交状态**，可干净启动 Commit 4（page_num / Assembly drift）。
两个已知风险（来自用户）：
1. page_num 不稳定（测试假设有 page_num，真实 parser 可能缺失）
2. hydrate 改写 identity 禁止（hydrateChunk:720-724 不再动 docId；正确方向 `_docKeys` + `pageClientKeys`）

当前链路：
```
Commit 1   Rename → InvoiceDocument          ✅
Commit 1b  Pack naming safety               ✅
Commit 2   assembled_documents contract     ✅
Commit 2.5 Assembly multi-page content      ✅
Commit 2.6 Backend observability            ✅
Commit 3   DocumentSelector 收口            ✅
Commit 2.7 amount 真源统一 (domain)         ✅  ← 本次
Commit 2.8 导出金额归一化 (format)          ✅  ← 本次
Commit 4   Assembly drift / page_num        ⏳
```

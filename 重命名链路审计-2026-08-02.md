# 重命名链路审计 — Rename Domain 未迁移到 InvoiceDocument

> 日期：2026-08-02 · 范围：只审计，未修改任何生产代码
> 触发现象：**文件列表显示一张发票（正确），重命名预览显示两条且金额不同（错误）**
> 关联文档：`同票多页链路审计-2026-08-02.md`（Assembly 层审计）

---

## 一、结论先行

你的判断是对的，而且比你预期的更准确：

> **同票多页的业务聚合没有失败。失败的是 Rename Domain 的数据源选择。**

- ✅ **可能1（重命名预览直接读 pages）—— 命中，且是根因**
- ✅ **可能2（金额不同）—— 命中，但不是"重新执行提取"，是"从未使用装配结果"**
- ✅ **可能3（rename 使用旧 identity）—— 命中，且牵出了被冻结的 P8**

而且**一键打包（Pack）比重命名更严重**：它连 `groupFilesByDocument` 都没调，是纯裸 page 数组。

---

## 二、消费端矩阵（本次审计的核心产出）

前端存在**两套并行的 Document 获取方式**，各消费端各自选择，无统一出口：

| 消费端 | 入口位置 | 数据源 | 层级 | 同票多页（2页）表现 |
|---|---|---|---|---|
| 侧栏 / 文件列表 | `Sidebar.jsx:110-115` | `documentView.documents` → 回退 `groupFilesByDocument` | **Document** ✅ | 1 行 ✅ |
| 预览 / 翻页 / GC | `App.jsx:117-122` | 同上（displayFiles） | **Document** ✅ | 1 条 ✅ |
| Excel 导出 | `App.jsx:580-583` | `displayFiles.filter(parsed)` | **Document** ✅ | 1 行 ✅ |
| **重命名预览** | **`useRenamePack.js:28`** | **`groupFilesByDocument(files)` 直连** | **Page** ❌ | **2 行** ❌ |
| **一键打包** | **`useRenamePack.js:351`** | **`files.filter(parsed)`，无任何聚合** | **Page** ❌❌ | **2 个** ❌ |
| PDF 导出确认弹窗 | `App.jsx:1203` | `files.filter(parsed)` | Page ❌ | 2 条 |
| 打包确认弹窗 | `App.jsx:1225` | `files.filter(parsed)` | Page ❌ | 2 条 |

**分界线非常干净**：凡是走 `displayFiles` / `documentView` 的都对；凡是自己从 `files` 起手的都错。

`App.jsx:283`：

```js
useRenamePack({ files, settings, setFiles, parseFiles, parseProgress, electronAPIRef })
//              ^^^^^ 收到的是 page-level 原始数组
//              既不是 displayFiles，也不是 documentView.documents
```

Rename 域**从未拿到过装配结果**。它连 `invoiceDocs` 的引用都没有。

---

## 三、🔴 断点 A：Rename 用 `groupFilesByDocument`，而它的归组键已被改写

### 现象机制

`useRenamePack.js:27-30`

```js
function collectDocumentLevelFiles(files) {
  const documentFiles = groupFilesByDocument(files)   // 按 f.docId 归组
  return documentFiles.filter(f => f.status === 'parsed')
}
```

`groupDocuments.js:68` 归组键是 `f.docId`。头部注释（:18-22）明确假设：

> docId 是内容哈希…**多页 PDF 所有页共享父 PDF 的 docId**

**这个假设在生产中已经不成立。** `useFileOps.hydrateChunk:720-724`：

```js
if (item?.docId && fileObj.docId !== item.docId) {
  const enriched = updateDocumentIdentity(fileObj, item.docId)
  fileObj.docId = enriched.docId        // ← 逐页改写为各自的物理内容哈希
  fileObj.identity = enriched.identity
}
```

拆页后每页是**独立的单页 PDF**，各有自己的 sha256。于是：

```
page1.docId = HASH_A   ─┐
                        ├─ 两个不同的 docId → 两个 group → 两行
page2.docId = HASH_B   ─┘
```

### 探针实证（已运行，非推理）

```
--- 生产态（逐页 docId 改写后）---
  条目数 = 2
   [0] name=invoice.pdf pageCount=1 amount=1000.00 isGroup=true
   [1] name=invoice.pdf pageCount=1 amount=300.00  isGroup=true   ← 精确复现用户症状

--- 测试态（fixture 假设共享 docId）---
  条目数 = 1
   [0] name=invoice.pdf pageCount=2 amount=1000.00 isGroup=true
```

**两条、且金额分别 1000 / 300** —— 与你描述的现象逐字吻合。

### 为什么文件列表是对的

`invoiceDocumentToRow`（`invoiceDocumentViewModel.js:43-55`）**没有踩这个坑**，因为它用的是 assembly 记录的精确页面 key 名单：

```js
// _pageKeys 是 assembly 阶段精确记录的页面 fileObj key 列表（强身份），
// 直接对 allFiles 命中，不依赖可能被 per-page render id 覆盖的 docId。
if (Array.isArray(invoiceDoc._pageKeys) && invoiceDoc._pageKeys.length) { ... }
```

**这段注释是本次审计中最有价值的一行代码**——写它的人已经意识到 docId 会被逐页覆盖，并为文件列表打好了护栏。问题是这个护栏**只装在了文件列表这一条路上**，Rename 走的是另一条没有护栏的路。

> 👏 值得肯定：`invoiceDocumentViewModel.js` 的强身份/弱身份双路设计、`invoiceDocumentsToRows` 的 O(n) 预建索引、以及 `_pages.sort((a,b)=>(a.pageNum ?? 0)-(b.pageNum ?? 0))` 里对 `pageNum=0` falsy 陷阱的显式防御，都写得很扎实。

---

## 四、🔴 断点 B：金额差异的真实来源（不是重新提取）

你猜是"重命名重新执行了字段提取"。**实际更简单也更根本：它读的一直是页级金额，从来没读过装配金额。**

`useRenamePack.js:4-25` → `buildInvoiceFields(f)`：

```js
amountHj: inv.amountHj || f.amount || f.totalAmount || '',
//                        ^^^^^^^^ 每个 page fileObj 各自的解析金额
```

后端 `get_batch_results`（`import_batch_manager.py:413-424`）是 **per-job（即 per-page）** 返回的：

```python
items.append({
    'clientKey': ...,
    'amount': result.get('amount'),   # ← 单页解析金额
    ...
})
```

于是 page1.amount=1000（首页合计）、page2.amount=300（续页局部）→ 两个文件名带不同金额。

---

## 五、🔴 断点 C：即使修好行数，金额仍会错（会存活于"天真修复"）

这是本次审计**最容易被漏掉**的一点，请务必在修复前读完。

### 三方冲突

**1. 装配规则说：金额取末页**

`invoice_assembly_pipeline.py:229`

```
多页情况：首页身份字段 + 全页明细 + 末页金额
```

**2. 前端行取的是首页**

`invoiceDocumentViewModel.js:76-88`

```js
const sorted = [...pageFiles].sort((a,b) => (a.pageNum ?? 0) - (b.pageNum ?? 0))
const rep = sorted[0]        // ← 首页
return { ...rep, ... }       // ← 行的 amount = 首页 amount
```

**3. 装配金额根本没过网线**

`import_batch_manager.py:801-807`

```python
batch.assembled_documents.append({
    'instanceId':    bucket_key,
    'sourceDocId':   src_doc_id,
    'invoiceNumber': inv_doc.get('invoice_number', ''),
    'invoiceType':   inv_doc.get('invoice_type', ''),
    'pageCount':     len(pages) if isinstance(pages, list) else 0,
})
# 没有 amount、没有 invoiceDate、没有 pageClientKeys
```

后端算出了正确的装配金额，写进了 DB（`inv_db`），**然后把它丢了**。

### 后果

如果只把 `useRenamePack` 的数据源换成 `documentView.documents`：

```
修复前： 2 行，金额 1000 / 300      ← 用户现在看到的
天真修复：1 行，金额 1000（首页值）  ← 行数对了，金额仍未必对
正确终态：1 行，金额 = 装配末页值
```

**行数问题和金额问题必须分开修**，前者在前端，后者要先扩后端契约。

---

## 六、🟡 断点 D：同一个测试反模式，第二次出现

`groupDocuments.test.js:41`

```js
const files = [page('AAA', 1), page('AAA', 2), page('AAA', 3)]
//                   ^^^^^        ^^^^^         ^^^^^  三页共享同一 docId
```

生产中三页的 docId 各不相同。**fixture 编码了一个 drift 之前的数据形状**，于是测试恒绿、生产恒错。

这与上一份审计发现的后端问题**是同一个反模式的第二个实例**：

| 位置 | fixture 假设 | 生产真相 | 测试状态 |
|---|---|---|---|
| `test_invoice_assembly.py:20-41` | page 带 `page_num` | parse 结果无此字段 | 🟢 假绿 |
| `groupDocuments.test.js:41` | 多页共享 `docId` | 逐页改写为各自哈希 | 🟢 假绿 |

**这已经不是偶然，是流程缺陷。** 建议立一条纪律：

> **Fixture 不得手工拼造上游不产出的字段形状。**
> 跨层测试的 fixture 必须由上游真实产物构造，或至少有一个契约测试锁住"上游确实产出这些键"。

---

## 七、🟡 与 P8（Identity ≠ Capability）的关系 —— 冻结状态需要重新评估

这是本次审计的**架构级发现**。

`hydrateChunk:720-724` 把**渲染身份**（每页物理 PDF 的 sha256）写进了 `fileObj.docId`。而 `fileObj.docId` 同时被 `groupFilesByDocument` 当作**业务归组身份**使用。

这正是 P8 的定义：**Capability 身份污染了 Identity 字段。**

代码注释（:717-719）也承认了动机是渲染侧：

```
// 确保后续 assembly 阶段从 readyFiles 中取到的 f.docId 都是真实物理 docId，
// 避免 resolveDocId() 优先取 identity.docId 得到旧父 PDF sourceDocId，导致预览URL错误一直加载。
```

为了修预览 URL，覆盖了业务归组键。`invoiceDocumentToRow` 用 `_pageKeys` 绕开了它，`groupFilesByDocument` 没有。

**建议**：P8 的冻结理由是"功能良性、纯架构级"。**这个前提已经失效**——它现在正在产生用户可见的重命名缺陷。建议把 P8 从"冻结观察"上调为"有已知功能影响的架构债"，但**仍不建议现在做 Phase A 全量迁移**（纪律仍然有效），而是走本地化修复。

---

## 八、🟡 其他发现

**1. `collectDocumentLevelFiles` 的过滤时序**（`useRenamePack.js:28-29`）

先 group 再 `filter(status==='parsed')`，过滤判据是 **representative（首页）的状态**。若首页 parsed 而续页 error，整票会被当作可重命名——`handleRenameConfirm:256-270` 会去重命名一个未解析完成的页。低概率，但语义上应该是"全页 parsed 才可重命名"。

**2. `handleRenameConfirm` 的多页分支写得是对的**（`useRenamePack.js:254-270`）

```js
if (doc._isDocumentGroup && Array.isArray(doc._pages) && doc._pages.length > 1) {
  const pages = [...doc._pages].sort((a,b) => (a.pageNum ?? 0) - (b.pageNum ?? 0))
  const pageSuffix = isFirstPage ? '' : `_p${(page.pageNum ?? 0) + 1}`
```

**多页重命名的下游逻辑是完备的**，包括对 `pageNum=0` falsy 陷阱的注释和防御。它只是永远收不到 `_pages.length > 1` 的输入。**修好数据源，这段代码立刻就能正常工作**——这是个好消息，改动面比想象的小。

**3. `docFacts.shouldAppendPageSuffix`（`layout/docFacts.js:60`）无人调用于 rename**

该函数的 docstring 明确写着：

```
所有消费方（Rename / 导出 / 一键打包 / 历史记录 / 缓存）统一调用本函数。
```

但 `useRenamePack.js` 里是手写的 `_p${n+1}` 拼接，没有调它。**文档宣称的统一出口并未落实**——同一规则有两份实现。

---

## 九、九阶段模型表（Rename 分支）

| # | 阶段 | 位置 | 输入模型 | 输出模型 | 保持 instanceId | 可能产生 invoice |
|---|---|---|---|---|---|---|
| 1 | 拆页 | `/split_pdf` | 父 PDF | N 个单页 PDF | — | 否 |
| 2 | 页解析 | `parse_invoice_service` | 单页 | PageParseResult | — | 否 |
| 3 | 暂存 | `page_result_store` | PageParseResult | bucket(instance_id) | ✅ | 否 |
| 4 | 装配 | `invoice_assembly_pipeline` | pages[] | InvoiceDocument | ✅ | **是** |
| 5 | 契约下发 | `assembled_documents` | InvoiceDocument | 5 字段 DTO | ✅ | 否（**有损**）|
| 6 | 前端 hydrate | `hydrateChunk:753-844` | DTO + files | Document + `_pageKeys` | ✅ | 否 |
| 6b | **docId 改写** | `hydrateChunk:720-724` | fileObj | fileObj(docId=页哈希) | ❌ **破坏归组键** | 否 |
| 7a | 列表视图 | `invoiceDocumentsToRows` | Document | Row（`_pageKeys` 强身份）| ✅ | 否 |
| 7b | **Rename 视图** | `groupFilesByDocument` | **裸 files** | **Row（docId 归组）** | ❌ | **是（伪造 N 张）** |
| 8 | 名称生成 | `preview-rename-names` | Row.invoiceFields | 文件名 | — | 否 |

**分叉发生在 7a/7b。** 6b 是使 7b 失效的直接原因。

```
                    InvoiceDocument (assembly ✅)
                            │
              ┌─────────────┴─────────────┐
              ↓                           ↓
      7a 列表/预览/Excel            7b 重命名/打包
      _pageKeys 强身份              groupFilesByDocument(files)
              │                           │
              ↓                           ↓
          1 行 ✅                      2 行 ❌
                                    （docId 已被 6b 改写）
```

---

## 十、建议修复顺序（未执行 · 严格 bisect：一步一 commit 一验证）

### 阶段一：让测试先变红（不改生产代码）

**R1.** 修 `groupDocuments.test.js` fixture，让多页用例使用**逐页不同的 docId**（贴合生产）。
预期：Case 1 由绿转红。**红了才有验收信号。**

**R2.** 补一条契约测试：锁住 `hydrateChunk` 后 `fileObj.docId` 逐页唯一这一事实。
防止未来有人"修复" fixture 时反向改回旧假设。

### 阶段二：修行数（前端，改动面小）

**R3.** `App.jsx:283` 把 `documentView`（或 `invoiceDocs`）传入 `useRenamePack`。

**R4.** `collectDocumentLevelFiles` 改为优先消费 `documentView.documents`，
`groupFilesByDocument` 仅作无装配结果时的回退——**与 `Sidebar.jsx:110-115` 完全对齐**。
> 建议直接抽一个共享的 `selectDocumentRows(documentView, files, isSearching)`，
> 让 Sidebar / App / Rename / Pack 四处共用，从结构上杜绝再次分叉。

**R5.** 同样处理 `handlePack`（:351）、`PdfExportConfirmModal`（App.jsx:1203）、
`PackConfirmModal`（App.jsx:1225）。

验收：三页同票 → 重命名预览 1 条、打包 1 个。

### 阶段三：修金额（需扩后端契约）

**R6.** 扩 `assembled_documents` 契约（`import_batch_manager.py:801-807`），
增加 `amount` / `invoiceDate` / `pageClientKeys`。
> 顺带解决上一份审计的 P1「有损契约」和断点③的根因，一箭三雕。

**R7.** `invoiceDocumentToRow` 用装配值覆盖 rep 的业务字段：
```js
return { ...rep, amount: invoiceDoc.amount ?? rep.amount, ... }
```

验收：多页发票文件名金额 = 末页合计，而非首页值。

### 阶段四：清理

**R8.** `useRenamePack` 的 `_p${n+1}` 改调 `docFacts.shouldAppendPageSuffix`，落实统一出口。

**R9.** `collectDocumentLevelFiles` 的 parsed 过滤改为"全页 parsed"语义。

### ⚠️ 不建议做的事

- **不要**现在动 `hydrateChunk:720-724` 的 docId 改写。它是为修预览 URL 加的，
  牵一发动全身，属于 P8 Phase A 范畴。**当前应绕开它（用 `_pageKeys`），而不是回滚它。**
- **不要**把本次修复和上一份审计的 Assembly 层修复混在一个 commit。
  两者独立：Assembly 决定"能不能装配成功"，Rename 决定"装配成功后有没有被用上"。

---

## 十一、与上一份审计的关系

| | Assembly 层审计 | 本次 Rename 层审计 |
|---|---|---|
| 问题 | 装配**能否成功** | 装配成功后**有没有被消费** |
| 症状 | 部分发票根本装不起来 | 装起来了但 rename 不用 |
| 当前用户可见现象 | 未直接可见（被列表层掩盖）| **就是这次的 bug** |
| 优先级 | P0，但可延后 | **P0，且应先修** |

**两条线独立，建议先修 Rename（用户可见、改动面小、验收清晰），再回头处理 Assembly。**

上一份审计的探针显示：`「共2页第1页」` 写法能正确装配成 1 组。你现在文件列表显示正确，说明你手上这份测试 PDF **恰好命中了能装配成功的路径**——这也印证了 Assembly 层的问题是"部分失效"而非"全失效"。

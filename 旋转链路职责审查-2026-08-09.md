# 旋转状态全链路审查报告（2026-08-09）

> 审查范围：文件列表 → 展示区（PreviewCanvas）→ 打印预览区（PrintPreviewCanvas）的旋转状态数据流。
> 原则：纯审查，不改代码。职责分明：**内存 fileRotations = 会话权威；DocFacts = 跨会话恢复；L2 缓存 = 派生快照**。
> 验收场景：导入 A、B → 旋转 A 90° → 切 B → 切回 A → A 展示区必须保持 90°（而非初始态）。

---

## 一、旋转状态全景：三源一权威

```
                     ┌─────────────────────────────────────────────┐
                     │  fileRotations (React state)                │
                     │  key = file.key (会话 UI key, 含时间戳)      │  ← 权威源
                     │  { [fileKey]: rotationDeg }                 │
                     └───────┬───────────────────────┬─────────────┘
                             │ 写入(2)                │ 读取(5)
              ┌──────────────┴───────┐        ┌───────┴──────────────────────────────┐
              │ handleRotate (L366)  │        │ ① previewRotation  (L559) → RenderCmd│
              │ doLoadPreview(L1554)⚠️│        │ ② currentRotation  (L539) → Canvas    │
              └──────────────┬───────┘        │ ③ usePrint (181/538/1151) → 打印图    │
                             │                │ ④ buildPrintExecutionPlan (264)       │
                             ▼                │ ⑤ exportSnapshotBuilder (88)         │
              ┌──────────────────────┐        └──────────────────────────────────────┘
              │ DocFacts.json        │
              │ key = docId          │ ← 跨会话恢复（Initialize Once）
              │ (sha256(bytes)[:24]) │    loadDocFacts → computeInitialDocFacts
              └──────────────────────┘
```

**三个存储的 key 完全不同**，职责必须严格分离：

| 存储 | Key | 职责 | 生命周期 | 正确优先级 |
|------|-----|------|---------|-----------|
| `fileRotations`（内存） | `file.key`（UI key） | 会话内用户旋转即时生效 | 会话 | **优先**（用户刚操作过） |
| `DocFacts.json`（持久） | `docId`（内容哈希） | 跨会话恢复旋转/纸张方向 | 永久 | 仅当内存无该 key 记录时 |
| L2 `fullCacheRef`（内存） | `cacheKey`（fileKey+rotation+layout） | 渲染结果快照 | 会话（LRU 10） | 派生（依赖前两者） |

---

## 二、展示区链路（文件列表 → PreviewCanvas）

```
点击文件
  └─ handlePreview (L1714)          [150ms 防抖 + version 守卫]
       └─ doLoadPreview (L1476)
            ├─ rotation 初值 = fileRotationsRef[key]   (L1519)  ← 读内存 ✅
            ├─ loadDocFacts(docId) → computeInitialDocFacts
            ├─ ⚠️ rotation = init.contentRotation      (L1555)  ← 持久层覆盖内存 ❌
            ├─ ⚠️ setFileRotations(key, init.contentRotation) (L1554) ← 覆盖内存 ❌
            ├─ documentStateRef.current = {...}        (L1566)  ← contentRotation=init
            ├─ cacheKey = buildPreviewCacheKey({fileKey, rotation})  ← 用覆盖后的 rotation
            ├─ L2 命中 → l2Command=buildRenderCommand(paperLayout, documentStateRef.current)  ⚠️ 无 previewRotation 覆盖
            └─ 未命中 → setPreviewFile → render effect
                              │
render effect (L618)
  ├─ previewRotation = fileRotations[previewFile.key] (L559)  ← 读内存（权威）
  ├─ renderCommand = buildRenderCommand(paperLayout,
  │      {...documentStateRef.current, contentRotation: previewRotation})  ← ✅ 显式覆盖
  ├─ reRotateSupported = (previewRotation===0)  → rotation≠0 强制走 Canvas
  ├─ Canvas 路径: switchPreviewFile(pdfDoc, 1, signal, currentRotation)
  │     → buildSingleFileRenderCommand({rotation}) → drawRenderCommand (contentRotation 旋转)
  ├─ RE 路径: buildRenderSpec(renderCommand) → getRenderEnginePreviewUrl → <img>
  └─ contentLayout (L1071):
        effW/effH = renderCommand.paperLandscape ? 交换 : 原样   ← 容器横竖
        imageRect = placement.offsetX/Y + docSize×scale          ← 内容定位
              │
              ▼
PreviewCanvas.jsx
  ├─ <img> 路径 (L72): 后端已按 spec 出图，objectFit:contain，无 CSS rotate ✅
  └─ canvas 路径 (L132): 旋转已烤进 canvas，直接 drawImage ✅
```

**展示区正确性**：主渲染路径（render effect）用 `previewRotation`（内存权威）显式覆盖 `contentRotation`，Canvas/RE 双路径一致。**问题只在 doLoadPreview 的 L2 缓存分支**（见 P0/P1）。

---

## 三、打印预览区链路（独立于展示区）

```
打开打印确认弹窗
  └─ usePrint: fileRotations[f.key] (L181/538/1151)   ← 读内存（权威）
       └─ buildPrintExecutionPlan (L264) / buildLegacyPrintPlan (L76)
            └─ perFileRotation(f) = fileRotations[f.key]
                 └─ slot.contentRotation / slot.rotation
                      └─ PrintPreviewModel (L205): userRotation = slotDef.rotation
                           └─ RotationResolver.resolveContentPlacement(contentRotation)
                                └─ renderTransformMM {translate, scale, rotationDeg}
                                     └─ PrintPreviewCanvas SlotImage SVG transform (L54)
```

**打印预览区完全消费 `fileRotations`**，不经过 `doLoadPreview` 的覆盖逻辑，也不依赖 DocFacts。
→ **打印预览区天然满足验收场景**（只要 fileRotations 在内存里是 90，打印预览就是 90）。

---

## 四、验收场景逐帧推演（当前代码）

导入 A、B（无持久记录，auto 模式）：

| 步骤 | 操作 | fileRotations | DocFacts | 展示区结果 |
|------|------|--------------|----------|-----------|
| 1 | doLoadPreview(A) | A=0 | 写入 A→0 | A 初始态 ✅ |
| 2 | doLoadPreview(B) | B=0 | 写入 B→0 | B 初始态 ✅ |
| 3 | 旋转 A 90° | **A=90** | save A→90 | A 显示 90° ✅ |
| 4 | 切到 B | B=0 | — | B 正常 ✅ |
| 5 | 切回 A（**保存成功**） | A=90 被 init=90 覆盖 → 90 | 读到 90 | A 显示 90° ✅ |
| 5′ | 切回 A（**保存失败/异步未完成**） | A=90 **被 init=0 覆盖 → 0** | 读到 0/空 | **A 显示初始态 ❌** |

**结论**：步骤 5′ 是当前代码的缺陷路径。`doLoadPreview` 无条件用 `init.contentRotation` 覆盖内存值，导致验收场景**依赖持久层写入的异步完成时序**。一旦 `saveDocFacts`（fire-and-forget `.catch(()=>{})`）失败或旋转后立即切换（IPC 未返回），A 的 90° 就丢失。

---

## 五、问题清单

### 🔴 P0：doLoadPreview 用持久层无条件覆盖内存 fileRotations（L1554-1555）
```javascript
let rotation = (fileRotationsRef.current[loadedFile.key] || 0)   // L1519 先读内存 90
...
setFileRotations(prev => ({ ...prev, [loadedFile.key]: init.contentRotation }))  // L1554 覆盖
rotation = init.contentRotation                                     // L1555 覆盖
```
**为什么错**：先读内存又用持久层覆盖，把"会话内用户操作"的优先级让给了"跨会话恢复数据"。持久层是异步的、可失败的（P2），内存才是会话内即时权威。
**修复方向**（建议，未实施）：
```javascript
const memoryRotation = fileRotationsRef.current[loadedFile.key]
const effectiveRotation = memoryRotation != null ? memoryRotation : init.contentRotation
rotation = effectiveRotation
setFileRotations(prev => ({ ...prev, [loadedFile.key]: effectiveRotation }))
```
语义：内存有记录 → 用户本会话操作过 → 用之；内存无 → 跨会话恢复。

### 🔴 P1：`documentStateRef.current.contentRotation` 不随 handleRotate 更新
`handleRotate`（L364）只 `setFileRotations` + `saveDocFacts`，**不更新 `documentStateRef.current.contentRotation`**。而 L2 缓存命中路径（L1592）构造 `l2Command = buildRenderCommand(paperLayout, documentStateRef.current)` **没有** `contentRotation: previewRotation` 显式覆盖（主渲染路径 L569 有）→ 旋转后 L2 命中构造的 RE URL spec 用陈旧 contentRotation，缓存键错位。
**修复方向**：handleRotate 里同步 `documentStateRef.current.contentRotation = deg`；或 L2 构造也显式覆盖 `contentRotation: rotation`。

### 🟡 P2：handleRotate 持久化 fire-and-forget + catch 吞错（L381）
```javascript
api.saveDocFacts(factKey, {...}).catch(() => {})   // 失败静默
```
旋转状态丢失无任何可观测信号（用户切回才发现）。至少 `console.warn` 错误；理想是 await + 失败提示。

### 🟡 P3：双轨 key 的职责混淆
`fileRotations` 用 `file.key`（UI key），DocFacts 用 `docId`（内容哈希）。同一文件**重新导入**（新 key）→ 内存无记录 → 只能靠 DocFacts（这是跨会话设计的正确意图）。但 P0 的覆盖逻辑让**同 key 切换**（会话内 A→B→A）也错误依赖持久层 → 职责混淆。

### 💭 P4：残留诊断日志（已清 DIAG-1/3/5/6/7/8/9/X/X2，待清以下）
| 位置 | 日志 |
|------|------|
| usePreview.js:72 | DIAG-2 fileRotations changed |
| PrintPreviewModel.js:208 | DIAG-4 printPreview rotation |
| PreviewCanvas.jsx:64 | DIAG-10 display layer |
| PrintPreviewModel.js:312/326 | DIAG-11/12 |
| PrintPreviewCanvas.jsx:59/91 | DIAG-13/15 |

---

## 六、职责划分结论（冻结语义）

1. **`fileRotations` = 会话内旋转的唯一权威**。所有消费方（展示区 renderCommand、Canvas 渲染、打印计划、打印图片、导出）都读它 —— 现状正确。
2. **`DocFacts` = 跨会话恢复**。仅在内存无该 file.key 记录时作为初始值 —— 现状错误（无条件覆盖）。
3. **`documentStateRef.current.contentRotation` = 派生镜像**，应始终与 fileRotations[当前文件] 一致 —— 现状在旋转时不更新（P1）。
4. **L2 缓存键** 必须与渲染输入完全一致 —— 现状 L2 分支用 documentStateRef（陈旧）而非内存权威（P1）。
5. **打印预览区** 与展示区共享 `fileRotations` 权威源，天然满足验收 —— 无需改动。

---

## 七、验收场景的修复后预期

修复 P0+P1 后：

```
导入 A、B → 旋转 A 90°（内存 A=90，DocFacts A→90）
→ 切 B（fileRotations[B] 无变化，B=0）
→ 切回 A：
   doLoadPreview: memoryRotation = fileRotations[A.key] = 90 → effectiveRotation = 90
   （不读持久层，或读但以内存为准）
   documentStateRef.contentRotation = 90（L2 路径也一致）
   cacheKey = {A.key}_r90@... → L2 命中旋转后快照（若存在）
   renderCommand contentRotation=90 → Canvas/RE 均按 90° 出图
→ A 展示区显示 90° ✅（不依赖 saveDocFacts 时序）
```

即使 `saveDocFacts` 失败（P2 仍静默），会话内 A→B→A 也能保持 90°；跨会话（重启）才回落到持久层状态。

# 文件列表展示旋转与打印预览旋转分离 — 方案

> 日期：2026-08-05 · 状态：方案（未改码） · 触发：用户反馈「文件列表中的旋转是对展示区进行旋转，而非打印确认页的预览旋转」

## 1. 问题

文件列表行右侧的「旋转」按钮（`FileCardRow.handleRotate`）目前是**全局旋转**——修改 `fileRotations[file.key]` 后，以下三个消费方**全部跟随**：

| 消费者 | 位置 | 效果 |
|---|---|---|
| **展示区** Canvas | `usePreview` currentRotation / previewRotation | ✅ 展示区缩略图旋转（正确） |
| **打印预览** PrintPreviewModel | `plan.slots[].rotation` → preview slot.rotation | ❌ 预览槽位跟着旋转（用户认为不应） |
| **打印执行** doPrint/executePrint | `buildPrintSettings.rotation` / canvas compose rotation | ⚠️ 打印内容跟着旋转（语义待定） |

用户期望：「文件列表旋转 = 展示区看方便，打印确认页不应跟着转」。

## 2. 现状调查

### 2.1 状态流

```
usePreview (owner)
  ├── useState({}) fileRotations
  ├── handleRotate → setFileRotations(prev => {...[key]: deg})
  ├── api.saveDocFacts(docId, { contentRotation: deg })   ← persist
  │
  └── return { fileRotations, ... }
        ↓
     App.jsx 解构 + 透传
        ↓
     usePrint({ fileRotations })
        ↓
     createPrintPlanInput(files, settings, fileRotations)
        ↓
     buildPrintExecutionPlan → plan.slots[].rotation = fileRotations[f.key] || 0
        ↓
     ┬ PrintPreviewModel → preview slot.rotation（预览槽位旋转）
     ├ doPrint → buildPrintSettings.rotation（Sumatra rotate=N）
     └ executePrint → printSingleSource fileRotation（Sumatra）
```

`fileRotations` 是**唯一状态源**，从展示区 hook 泄漏到两条打印链。

### 2.2 与既有冻结边界的关系

- 早期冻结：「单票旋转属于展示阶段；进入打印预览后，没有内容旋转权限」——已通过删除 PrintConfirmModal 的 `contentRotation` prop 实现（确认页无旋转控件）
- 但 `fileRotations` 仍通过 `plan` 数据结构**数据层面泄漏**到预览（预览 slot 直接读 `slotDef.rotation`），用户视觉上看到「展示区旋转了 → 预览也转了」

### 2.3 打印执行 rotation 的去留判断

`fileRotations` 对打印执行的影响（`buildPrintSettings.rotation`）是**既有行为**：
- Sumatra 打印时 `rotate=N` 由 `resolveOrientationCommands(contentOrient, paperOrient, desiredRotation)` 决定，其中 `desiredRotation` = `fileRotations[f.key]`（来源：printSingleSource → buildPrintSettings）
- merge 打印时 canvas compose rotation 也来自 `fileRotations`

分离 `fileRotations` 从打印执行会**改变打印结果**（用户之前旋转文件 → 打印跟着转的既有行为消失）。是否应分离取决于用户对「打印旋转」的期望——属于独立决策，建议分阶段。

## 3. 方案：分阶段分离模型

### 3.1 目标模型

```
展示区旋转（fileRotations）           打印旋转（printRotations，独立）
    ├── 展示区 Canvas                    ├── 打印预览 slot rotation
    ├── api.saveDocFacts 持久化          └── 打印执行 rotation
    │
    └── ❌ 不进入打印预览 / 打印执行
```

### 3.2 Phase 1（立即）— 打印预览解除 fileRotations

**范围**：只动 PrintPreviewModel，不碰打印执行。

- `PrintPreviewModel.pageToModel`：slot `rotation` 不再来自 `slotDef.rotation`（fileRotations），固定为 `0`。
- 或者保留字段但标记为「打印旋转字段」（未来由 printRotations 填入），当前值为 `0`。
- 效果：文件列表旋转文件后，展示区跟着转，**打印预览不变**（内容保持原始方向）。

**改动文件**：
- `frontend/src/print/PrintPreviewModel.js`：L155 `rotation: slotDef.rotation || 0` → `rotation: 0`（或重命名为独立字段）
- 测试 `printPreviewModel.test.mjs` PM-06（rotation 断言）需要更新

**不做**：
- 不改 `buildPrintExecutionPlan`（plan 的 rotation 字段保留供打印执行使用）
- 不改 usePrint / doPrint / executePrint（打印执行继续消费 fileRotations）
- 不改 FileList、usePreview、App.jsx

### 3.3 Phase 2（评估后）— 打印执行 rotation 独立化

**前提**：用户确认「打印旋转」的期望（是否应独立于展示区旋转）。

- 如果用户确认分离：新增 `printRotations` 状态（owner=usePrint 或独立 hook），打印预览/打印执行消费 `printRotations` 而非 `fileRotations`；展示区旋转不再影响打印结果。
- 如果用户确认保持全局行为：Phase 1 即完成（展示区旋转只影响展示区，打印旋转仍来自 fileRotations 自动跟随——Phase 2 不做）。
- 此决策应与「方向旋转方案」协同评估（方向旋转方案中预览内容旋转来自纸张方向，不与 fileRotations 耦合）。

## 4. 与方向旋转方案的关系

方向旋转方案（`打印预览方向旋转调查与方案.md`）中，阶段 1「预览方向旋转适配」涉及 SlotImage 的旋转语义（先旋转后 fit）。两个方案的改动在 `PrintPreviewModel` 和 `PrintPreviewCanvas` 有交集：

**建议顺序**：先做本方案 Phase 1（打印预览解除 fileRotations）→ 再做方向旋转方案阶段 1（预览方向旋转适配）。原因：
- 本方案是**语义减法**（移除一个旋转来源），改动小、风险低
- 方向旋转是**语义加法**（增加方向驱动的旋转），依赖 slot rotation 字段的应用语义清晰
- 先「清空」slot rotation（归 0），再「填入」方向旋转（方向驱动的 rotate 90）

## 5. 影响面

| 文件 | 改动 |
|---|---|
| `PrintPreviewModel.js` | L155 `rotation: slotDef.rotation \|\| 0` → `rotation: 0`，注释说明打印旋转已分离（Phase 1） |
| `printPreviewModel.test.mjs` | PM-06 旋转断言更新（rotation 恒 0 或移除该断言） |
| 其余（FileList/usePrint/Sidebar/App） | 不动 |

## 6. 风险

- **打印执行**：Phase 1 不碰，打印结果不变（fileRotations 仍生效）——无风险
- **预览与打印一致性**：Phase 1 后「预览 slot.rotation 恒 0」但「打印 SUM rotation != 0」→ 预览与打印不一致——这是 Phase 1 的有意副作用（打印预览不再模拟打印旋转，留待打印旋转独立 UI）
- **用户感知**：展示区旋转后进入打印确认页，内容不再跟着转——符合用户期望

## 7. 测试计划

- PM-06 更新：`rotation` 改为 `0` 或删除（原断言 `rotation: 90`）
- 新增：横向+fileRotations 90 → 预览 slot rotation 仍为 0（不泄漏）
- 运行 printPreviewModel + printExecutionPlan 回归

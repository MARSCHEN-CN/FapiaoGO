# 合并模式打印逻辑审查（一页两票 / 三票 / 四票）

> 审查日期：2026-08-14
> 现象：合并模式（merge2/3/4）打印**均为失败**，但**打印预览正确**。
> 用户要求：严格审查、不动代码、打印效果需跟随预览。
> 更新（2026-08-14 20:xx）：按用户指正，将"逐节点证伪"纪律固化为调查基线；
> **H1 不再预设为根因**；新增 H3 代码级排除与"日志盲点"判定。

---

## 一、结论先行（修正版）

**不要把"预览正确"推断为"打印渲染成功"。** 预览与打印虽然调用同一个
`renderMultipleItemsToCanvas`，但上游输入（job 派生）、执行计划、产物消费方式都不同。
"预览正确、打印失败"只能证明渲染几何本身可行，**证明不了打印这一路拿到了有效 PNG**。

因此本审查遵循一条不可跳过的纪律：

> **先证伪 Job → Canvas → PNG 有效，再证伪 IPC → PDF → Sumatra 有效。**
> 每一步都必须是 `非 null / 非 0 / 非 undefined / 非 error`。
> 任一节点出现 `0 / null / undefined / error`，根因即收敛于该节点，**不必再往下查**。

---

## 二、两条链路对照（合并模式）

| 阶段 | 预览（正确） | 打印（失败） |
|---|---|---|
| 入口 | `usePreview.js` `renderToCanvas` → `renderMultipleItemsToCanvas` | `usePrint.js` `doPrint` → `renderMergeGroupToPrintImage` |
| 渲染函数 | 同一 `renderMultipleItemsToCanvas(items, …, false, …, {userMargins}, paperLayout)` | 同一 `renderMultipleItemsToCanvas(items, …, false, …, {无 userMargins}, printPaperLayout)` |
| 执行路径 | `isPrint=false` → `_renderViaWorker` | `isPrint=false` → `_renderViaWorker`（同一 Worker） |
| 合成器 | `canUseSlotComposer` 真 → `MultiTicketComposer` | 同一 `MultiTicketComposer`（printPaperLayout 同为 truthy） |
| 产物 | 屏幕 Canvas（正确） | Canvas → `toBlob` → `Uint8Array` PNG |
| 下游 | 无 | `ipc 'print-merged-images'` → 主进程 |
| 主进程 | 无 | PNG 魔数校验 → `batch-png-to-pdf`(Python) → `DirectPrintHandler`(Sumatra) |

关键事实：**渲染层预览与打印用同一函数、同一 Worker、同一 V16 合成器**，所以只要
传入参数一致，打印画出的内容应与预览逐像素相同。失败落在渲染之后的"再编码 / 提交"环节——
但具体是渲染产物本身为空，还是下游转换失败，必须**逐节点取证**，不能先入为主判 H1。

---

## 三、已确认：渲染层预览与打印一致（好消息）

1. **同一渲染函数** `renderMultipleItemsToCanvas`（`renderers.js:1022`）。
2. **同一 Worker，并发关联正确**：`_renderViaWorker` 用自增 `_msgId` + `_pendingRequests` 映射
   （`renderers.js:959-992`），多组合并发渲染不会拿错/错乱响应（H4 并发串扰已基本排除）。
3. **同一 V16 路径**：`canUseSlotComposer(paperLayout)` 仅判 `!!paperLayout`
   （`renderers.js:29-31`），预览 `paperLayout` 与打印 `printPaperLayout` 都为 truthy → 都走
   `MultiTicketComposer`。
4. **缓存键区分组合与预览**：`buildCacheKey` 含 `items.map(i=>i.key).join(',')`
   （`renderers.js:1019`），且预览传 `userMargins`、打印不传 → 两者 margin key 不同，
   故不同合并组、预览与打印不会撞键。

---

## 四、分叉 / 风险（当前未必是失败点，但违背"打印跟随预览"）

- **D1 — `isPrint` 未进缓存键（旧文档 Blocker 2 未修）**
  打印侧刻意传 `isPrint=false`（`usePrint.js:491`，注释"与预览保持一致"），因此打印走 Worker。
  `buildCacheKey`（`renderers.js:1013`）仍不含 `isPrint`。目前仅因"预览传了 userMargins、
  打印没传"导致缓存键不同才未串味——**脆弱**。→ 建议后续把 `isPrint` 纳入 `buildCacheKey`。
  **当前阶段不修**（用户已指示先不碰 D1/D2）。

- **D2 — `userMargins` 参数不一致**
  预览传了 `userMargins`（`usePreview.js:874`），打印没传（`usePrint.js:493`）。
  当前 V16 路径不消费 `userMargins`（slot 由 `paperLayout` 驱动），故暂时无害；
  但两路参数并非逐字节一致。→ 建议后续打印补传相同 `userMargins`。**当前阶段不修**。

- **D3 — 两侧 paperLayout 等价（不是分叉）**
  打印 `printPaperLayout` 与预览 `paperLayout` 用同一默认边距、同工厂，结构等价，
  `canUseSlotComposer` 两侧判定一致。已确认不是分叉点。

---

## 五、调查纪律：逐节点证伪（P0 → P1 → P2）

核心原则（用户指正，已采纳）：

> **"打印跟随预览"的最终架构应是：一个共享的 Final Canvas，Preview 与 Print 都从它派生。**
> 而不是 Print 再调用一次 `renderMultipleItemsToCanvas` 后又做一套独立几何 / 转换。

按真实链路证伪顺序：

| 优先级 | 假设 / 节点 | 状态 |
|---|---|---|
| **P0** | `deriveMergePrintJobs` → `j.files` 为空 / 错位 | **必须先证伪**（代码级 ID 映射已排除，但空组仍可能因 read-file 失败） |
| **P0** | `renderMergeGroupToPrintImage` → Canvas / PNG 无效 | **必须先证伪**（存在静默 `return null` 盲点，见 §六） |
| **P1** | IPC → `print-merged-images` 收到的 image 数量 / byte / PNG magic 异常 | 待验证 |
| **P1** | `batch-png-to-pdf` 转换失败（合并专属） | 待验证 |
| **P1** | PDF 正确但 `DirectPrintHandler.handle()` / Sumatra 提交失败 | 待验证 |
| **P2** | `userMargins` / cache key 分叉（D1/D2） | 架构风险，暂非直接根因 |
| **P2** | Worker 并发串扰（H4） | 已有较强代码证据排除 |

**判定短路规则**：只要 P0 任一节点出现 `j.files.length===0` / `canvas===null` /
`blob===null` / `png.length===0`，则 **H1（Python/Sumatra）直接排除**——因为根本还没走到那一步。

---

## 六、【关键新发现】H3 代码级排除 + 日志盲点

### 6.1 H3（slots.fileId 与 files.key 对不上）在代码层即被排除

- `buildPrintExecutionPlan.buildSlot`（`buildPrintExecutionPlan.js:284`）明确写 `fileId: f.key`。
- `deriveMergePrintJobs`（`deriveMergePrintJobs.js:27`）用 `new Map(files.map(f => [f.key, f]))` 反查。
- 二者 key 完全一致 → `fileById.get(s.fileId)` 必然命中。纯"ID 映射错配"型 H3 **不存在**。

### 6.2 更强的推理：plan 层若有错，预览必然也错

- 预览走 `buildPrintPreviewModel(plan, …)`（`usePrint.js:576`）；
- 打印走 `deriveMergePrintJobs(plan, files)`（`usePrint.js:715`）。
- 二者消费**同一个 `plan`**。若 plan 的分组 / 成员 / 方向有错，**预览不可能正确**。
- 用户确认预览正确 ⇒ plan 与 job 派生层基本可信 ⇒ 失败必然在 `deriveMergePrintJobs` **之后**。

### 6.3 ⚠️ 日志盲点（P0 无法用"现有日志"确认）

`renderMergeGroupToPrintImage`（`usePrint.js:412-509`）的失败出口**全部静默、零日志**：

- **L466** `if (validItems.length === 0) return null` —— 空组直接返回 null，无 log；
- **L499** `if (!blob) return null` —— `canvas.toBlob` 产空直接返回 null，无 log；
- L498 `canvas.toBlob(...)` 之前也**没有任何日志**确认 `canvas` 存在 / 尺寸。

`deriveMergePrintJobs`（`:26-41`）同样**零日志**。

→ 后果：若某组合并失败，渲染进程**不会打印任何报错**，与主进程 `print-merged-images` 也无关联
日志，看起来就是"静默失败"。这恰恰是"打印失败却无明显报错"的现象来源。
**结论：现有日志能力无法覆盖 P0 节点，必须加针对性 `console.log` 探针（见 §八）才能证伪 P0。**

> 注：`validItems` 为空仍可能发生在 PDF 分支 `read-file` 返回 `success:false` 时
> （`usePrint.js:419-423`：此时 pdf 分支不 return，落到末尾 `return null`，被 `filter(Boolean)` 剔除）。
> 虽单文件打印用同一 `read-file`，但合并组的 `printPath` 集合与单文件未必完全一致，仍属 P0 待证伪项。

---

## 七、根因假设重排（不再预设 H1 为根因）

### H1：`print-merged-images` 在 Python / Sumatra 阶段抛错 —— **待验证（P1），不再是"最可能"**
- 合并专属：单文件打印绕过 `batch-png-to-pdf` 与 `DirectPrintHandler` 合并分支。
- 但**仅当 P0 已证伪（PNG 有效）后**才有资格提升为首要怀疑。
- 可能子点：`batch-png-to-pdf` 转换失败；`DirectPrintHandler.handle` 返回失败。

### H3（重排）：`j.files` / `validItems` 为空组 —— **P0，代码层 ID 映射已排除，但空组仍待证伪**
- ID 错配已排除（§6.1）；
- 剩余风险：**read-file 失败 / 格式未识别 → 全组合并项为 null → 空组 → 静默 null**（§6.3）。
- 判据：§八 P0 探针的 `mergeJobs` 与 `validItems` 日志。

### H2：`renderMergeGroupToPrintImage` 在打印上下文返回 null（每组渲染失败）
- 子因① `read-file` 无 `.arrayBuffer()` → 所有 item 变 null → 空组（同 H3 子树）。
- 子因② Worker 返回空白/undefined canvas → `canvas.toBlob` 为 null → 返回 null。
- 二者均可用 §八 P0 探针的 `canvas?` / `pngBytes` 节点证伪。

### H4：`settings.printerName` 空 —— **P1，概率低**
- 单文件 source 打印也依赖同一 `settings.printerName` 且正常 ⇒ 此处应已就绪。
- 仅当 P0/P1 上游全绿、且主进程报"未选打印机"时才命中。

---

## 八、P0 探针（探针 A+B 已插入 `usePrint.js`，状态：已激活 / 仅日志，不改行为）

> 用户调整：探针不要只打印布尔值，要形成**可判定证据**（count / size）。
> 短路纪律：P0 任意一项 `0 / null / undefined` → 停止，不碰 Python、不查 Sumatra；
> 仅当 P0 全绿（PNG 均 > 0、canvas 尺寸正常）才进入 P1。

### 探针 A — `usePrint.js` `doPrint` 合并分支（证伪 job 派生 / H3）【已插入，L740-748】
位置：`const mergeJobs = deriveMergePrintJobs(plan, files)` 之后。
```js
console.log('[P0][JOB] mergeMode=%s groupSize=%d jobCount=%d', mergeMode, groupSize, mergeJobs.length)
mergeJobs.forEach((j, gi) => {
  const fs = j.files || []
  console.log('[P0][JOB] groupIndex=%d fileCount=%d fileKeys=%s',
    gi, fs.length, fs.map(f => f.key).join(','))
})
```
判据：`fileCount === 0` ⇒ 空组 ⇒ 证伪 job 派生（H3 子树），P0 短路。

### 探针 B — `usePrint.js` `renderMergeGroupToPrintImage`（证伪 Canvas / PNG / H2/H3）【已插入，L468-529】
- B-RENDER（input/valid）：`validItems` 过滤后
```js
console.log('[P0][RENDER] group=%s inputCount=%d validCount=%d', _groupId, group.length, validItems.length)
if (validItems.length === 0) { console.warn('[P0][RENDER] group=%s validItems=0 ⇒ 静默 return null', _groupId); return null }
```
- B-CANVAS（画布尺寸）：`renderMultipleItemsToCanvas(...)` 之后
```js
if (!canvas) { console.warn('[P0][RENDER] group=%s canvas=NULL', _groupId); return null }
console.log('[P0][RENDER] group=%s canvasWidth=%d canvasHeight=%d', _groupId, canvas.width, canvas.height)
```
- B-PNG（编码产物）：`canvas.toBlob` 之后
```js
console.log('[P0][PNG] group=%s blobSize=%d pngBytes=%d', _groupId, blob.size, _pngBytes.length)
```

> 判读（可判定证据，非布尔）：
> - `canvas === null` → 渲染失败（H2 子树）；
> - `canvas` 存在但 `0×0` → 几何/渲染产物异常；
> - `canvas` 正常尺寸但 `blob === null` → 编码阶段失败；
> - `pngBytes > 0` 且尺寸正常 → P0 完整通过。
> `_groupId` = 组内文件 `key` 拼接，用于跨 log 关联，避免依赖队列顺序。
> 注：B-CANVAS / B-PNG 的 `return null` 为**诊断性早返回**（原 `canvas.toBlob` 在 null 时会抛 TypeError 被上层 catch），
> 契约边界仍为返回 null，不溢出 `renderFn`，不改行为。

### 探针 C（P1）— `electron/main.js` `print-merged-images`（证伪 IPC→PDF→Sumatra）
仅在 P0 全绿后启用。`electron/main.js:~882` 处理器内：
```js
console.log('[P1] images.length =', images?.length)
images.forEach((img, i) => {
  const buf = toImageBuffer(img)
  const magic = buf.subarray(0, 8).toString('hex')
  console.log(`[P1] img#${i} len=${buf.length} pngMagic=${magic}`)
})
const batchResult = await callPython([...])
console.log('[P1] batchResult =', JSON.stringify(batchResult))
for (let i = 0; i < pdfPaths.length; i++) {
  const r = await DirectPrintHandler.handle(pdfPaths[i], settings)
  console.log(`[P1] DirectPrint#${i} =`, JSON.stringify(r))
}
```

---

## 九、理想架构（isPrint=false 方向正确，避免 Print 重算几何）

当前打印侧刻意 `isPrint=false`（复用预览渲染路径而非独立渲染），**方向正确**——
它保证打印走与预览相同的 Worker / 合成器，满足"打印跟随预览"的几何前提。

应避免的退化形态：
```
预览：renderMultipleItemsToCanvas → Canvas A
打印：renderMultipleItemsToCanvas → Canvas B → 又做一套独立几何 / 转换
```
最理想终态：
```
        Merge Group
            │
            ▼
   renderMultipleItemsToCanvas
            │
            ▼
        Final Canvas  ← 唯一真源
         ┌──┴──┐
         ▼     ▼
      Preview  PNG → PDF → Sumatra
      (<img>)        (Print 仅消费 Final Canvas，不再重算"该画什么")
```

即 Print 这条链路只做"Canvas → PNG → PDF → Sumatra"的**提交**，不重新决定版面。
当前 `renderMergeGroupToPrintImage` 仍各自调用一次 `renderMultipleItemsToCanvas`，
属可接受的中间态（因 `isPrint=false` 复用同源），但终极目标是 Preview/Print 共享同一产物。

---

## 十、下一步

1. **探针 A + B（P0）已插入 `usePrint.js`（2026-08-14）**，复现一次 merge2/3/4 打印，
   到渲染进程 DevTools Console（`Ctrl+Shift+I`）看 `[P0][JOB]` / `[P0][RENDER]` / `[P0][PNG]` 日志。
2. 若 P0 出现 `0 / null / undefined` → 根因收敛于 job 派生或渲染产物，先不动 Python；
   把日志贴回即可精确定位。
3. 若 P0 全绿（PNG 均 > 0、尺寸正常）→ 再插入探针 C（P1），到主进程终端看
   `print-merged-images` / `batch-png-to-pdf` / `DirectPrintHandler` 日志，此时才有资格查 H1。
4. **当前不修 D1/D2，不先动 Python**（用户指示）。

> 附：系统提示 `MEMORY.md` 超限需清理，属独立后台任务，不影响本次审查。

---

## 十一、P0 实证结论（merge4 复现，2026-08-14 20:45）✅ 根因已锁定

### 11.1 复现证据（一次 merge4 打印）
```text
[P0][JOB]   mergeMode=merge4 groupSize=4 jobCount=1
[P0][JOB]   groupIndex=0 fileCount=2
            fileKeys=[253220...865b, 253170...73a2]          ← job 派生 OK
usePrint.js:463 加载合并项失败: ...pdf
            TypeError: fileData.data.arrayBuffer is not a function   ← 真凶
usePrint.js:463 加载合并项失败: ...pdf
            TypeError: fileData.data.arrayBuffer is not a function
[P0][RENDER] group=... inputCount=2 validCount=0             ← 全组无效
[P0][RENDER] group=... validItems=0 ⇒ 静默 return null        ← 吞错命中
```

### 11.2 代码级根因（已确认，非推测）
`read-file` 处理器契约（`electron/ipc-file-ops.js:104-105`，注释 L102-103）：
```js
const buffer = await fs.promises.readFile(filePath)
return { success: true, data: buffer }   // Node Buffer → IPC 序列化为 ArrayBuffer/TypedArray
```
即 `fileData.data` 是 **ArrayBuffer/TypedArray**，**不是 Blob**。而 `renderMergeGroupToPrintImage`
（`usePrint.js:425`）写：
```js
return { ...f, _pdfData: new Uint8Array(await fileData.data.arrayBuffer()) }  // ❌ ArrayBuffer 无 .arrayBuffer()
```
`ArrayBuffer` 没有 `.arrayBuffer()` 方法 → 抛 `TypeError` → 被 `catch` 吞掉 → `item=null`
→ `validItems=0` → 静默 `return null` → 该组合并组无 PNG → 打印失败。

**这正是审查基线 §二点名的"静默 `return null` 吞错盲点"被实锤。** 预览正确是因为预览路径对
`read-file` 返回用**正确的** `new Uint8Array(fd.data)`（`usePreview.js:608/1414`），且 L918/1404 的
`.arrayBuffer()` 作用在 `Blob`（`previewFile.file`/`fObj.file`）上，合法。

→ **本 Bug = "打印跟随预览"原则被违反的具象化**：Print 重写了文件数据加载，且写错了；Preview 的同款加载是对的。

### 11.3 假设判定
| 假设 | 状态 | 依据 |
| --- | --- | --- |
| H3 纯 ID 错配 | ✅ 排除 | `fileCount=2`、key 正确（§四 代码证据） |
| H2 渲染几何失败 | ✅ 排除 | `renderMultipleItemsToCanvas` 从未被调用（`validItems=0`），canvas 未生成 |
| H1 `print-merged-images` 下游 | ✅ 排除 | P0 在文件加载即短路，**根本没生成 PNG**，Python/Sumatra 未触及 |
| Worker 并发串扰 | ✅ 排除 | 早已代码排除；本次单组即失败 |
| **R0：文件数据加载用错 API** | ✅ **根因** | `fileData.data.arrayBuffer()` 对 ArrayBuffer 非法 |

### 11.4 最小修复（预览一致写法，待用户授权）
3 处完全相同的同根错误，均改为 `new Uint8Array(fileData.data)`（与 `usePreview.js:608/1414` 一致）：

| 行 | 上下文 | 当前（错） | 改为 |
| --- | --- | --- | --- |
| `usePrint.js:194` | 单文件 PDF | `new Uint8Array(await fileData.data.arrayBuffer())` | `new Uint8Array(fileData.data)` |
| `usePrint.js:425` | **合并 PDF（本次失败点）** | `new Uint8Array(await fileData.data.arrayBuffer())` | `new Uint8Array(fileData.data)` |
| `usePrint.js:1173` | 另一 PDF data 构造 | `new Uint8Array(await fileData.data.arrayBuffer())` | `new Uint8Array(fileData.data)` |

注：`L62`/`L520` 的 `blob.arrayBuffer()` 作用在真正 Blob 上，合法，**不动**；
`L452`/`L461` 图片分支用 `new Blob([fileData.data])`，合法，**不动**。

修复后 P0 链路变为：`j.files`(OK) → `read-file`→`new Uint8Array(data)`(OK) →
`validItems>0` → `canvas`(OK) → `PNG>0` → IPC → PDF → Sumatra，与预览几何逐字节一致。

### 11.5 纪律边界（不越界）
- **不动 Python、不动 `DirectPrintHandler`、不动 Sumatra**（H1 已排除，无需碰）。
- **不修 D1（isPrint cache key）/ D2（userMargins 一致性）**——仍留 P2 架构债务，避免与本次定位混批。
- 删除 P0 探针（A+B）应在修复验证通过后、与修复分开提交，保持"定位"与"清理"两个独立批次。

### 11.6 修复落地状态（2026-08-14 20:5x）
- **Commit A 已创建（本地）**：`98a865fb` `fix: align print read-file data handling with IPC contract`
  —— `frontend/src/hooks/usePrint.js` 仅 3 行变更（`new Uint8Array(await fileData.data.arrayBuffer())`
  → `new Uint8Array(fileData.data)`，L194/L422/L1140），`3 insertions/3 deletions`，**不含探针**。
- 提交后已将探针从暂存副本（`/tmp/usePrint_fixed_probes.js`）恢复到工作区：`git diff --stat` 显示
  相对 HEAD 仅 38 行探针增改、`[P0]` 计数 13 → 探针留待验证用，符合"本批次不动 P0 探针"边界。
- **Push 未完成**：非交互沙箱中 `git push` 触发 GitHub `helper-selector` 交互式 OAuth 静默失败
  （`git ls-remote` 读可达，写需凭据）。待用户在可交互终端执行 `git push -u origin rotation-b1-hardening`
  （或提供 PAT）后同步远端。
- **待验证（用户手动）**：保留探针复现 merge2/3/4，确认 `[P0]` 全绿（fileCount>0 / validCount>0 /
  canvas>0 / pngBytes>0）且最终打印成功；通过后再做 **Commit B** `chore: remove temporary P0 merge print diagnostics`
  （单独提交，不与 A squash）。

---

## 十二、Stage 2 审查：几何分叉 + 辅助线（2026-08-14 21:2x）

Commit A 修复后跨过"文件加载失败"，进入"打印结果与预览视觉语义不一致"新阶段。
本阶段**只做代码审查、不动任何代码**，回答用户三个问题，并把问题拆成 Gate A / Gate B。

### 12.1 Q1 — Preview 的 merge fit scale 在哪里算？
- Preview `paperLayout` = `computePaperLayout({ paperSize, customPaper, margins:{top/right/bottom/left} })`（`usePreview.js:173-181`，输入来自 `settings.margin*`）。
- fit 几何在 `render.worker.js` → `iterateSlots` → `computeSlots` / `MultiTicketComposer` 由 `slots` 的 **`usableRect`** 推导 slot 摆放（`SlotLayout.js:156 computeTicketSlots`）；slot 内缩用 `slotMarginPx`（5mm）。
- `userMargins` **不进入** slot 几何（V16 path：MultiTicketComposer / composeSlot / composeSlotRasterizer 均零引用 `userMargins`），仅进 L2 cache key。

### 12.2 Q2 — Print 的 merge fit scale 在哪里算？
- Print `printPaperLayout` = `computePaperLayout({ paperSize, customPaper, margins:{left??3,right??3,top??3,bottom??3} })`（`usePrint.js:484-491`）。
- **同一 `computePaperLayout` 函数、同一 `settings.margin*` 输入**：`??3` 与 raw 在 `computePaperLayout.toPx` 中对非有限值都归一为 3（`:189-196`），故两者 `usableRect` **逐字节相同**。
- 同样走 Worker → `computeSlots` → `MultiTicketComposer`；`groupSize`、`forcedLandscape`、`isPrint=false`、`showSafeMargin=false` 两侧一致。
- **结论：Canvas 级 fit scale 两侧完全一致。**

### 12.3 Q3 — 分割线 / 安全边距线 到底画在哪里？（不猜，代码实证）
- **分割线（`#cccccc` 虚线）**：`render.worker.js:35-71` 在 **Final Canvas 里画**，`slots.length>1` 时**无条件**画（merge2/3/4 全命中），**不区分 `isPrint` / `showSafeMargin`**。
  → 它进 Preview canvas，也进 Print artifact。这是 **Render Pipeline 问题**（Gate B 违规：分割线属 UI aid，不应进 print artifact）。
- **安全边距线**：当前代码里**只在 `PrintPreviewCanvas.jsx:405-417` 的 SVG overlay**（Preview-only 虚线框，由 `hasMargins` 控制）；`render.worker.js` 的 grep 中**无任何 safe-margin 绘制分支**。
  → 按代码它**不会**进 print canvas。若用户确在打印件看到"安全边距线"，要么是把 Worker 分割线误认成安全边距线，要么需查实际打印 PNG 是否有其它 draw 源（属 P1 阶段取证）。

### 12.4 对用户强假设的严谨修正
用户原假设：*"D2 `userMargins` 不能再简单留 P2，很可能就是 fit 分叉原因"*。
**代码证据推翻该假设**：
1. V16 path 的 `computeSlots`/`MultiTicketComposer`/`composeSlot*` 对 `userMargins` **零引用** → 它不是 fit 输入；
2. Preview 与 Print 的 `paperLayout`/`printPaperLayout` 因 `??3` 与 `toPx` 默认值等价而 **`usableRect` 完全相同** → 也不是 paperLayout 构造差异。
⇒ fit 分叉**既非 userMargins、也非 paperLayout 构造**；Canvas 级几何两侧一致。若最终打印纸"没 fit 到边距"，分叉必然在 **P1 下游（PNG→PDF→Sumatra）**——即原 H1（`batch-png-to-pdf` 的 page size / orientation / margin 假设），现因 P0 已通过而重新回到候选首位。

### 12.5 Gate 拆分（沿用用户框架）
- **Gate A（几何必须与预览一致）**：Canvas 级已代码证明一致。验收标准升级为——
  `打印 PNG 的 slot 矩形` ≡ `预览 canvas 的 slot 矩形`（逐像素）+ `PDF 页面尺寸/orientation` ≡ `canvas 尺寸`（merge4 横向尤为关键）。若 canvas 一致而打印纸不 fit ⇒ 根因在 P1，不在渲染。
- **Gate B（artifact 不含辅助线）**：`safe margin geometry` 保留、`safe margin guide` 不打印（与冻结 Margin Contract 一致）。**已确认违规项**：Worker 分割线（`render.worker.js:35-71`）无条件进 artifact，需引入 `showGuides`/`includeSeparators` flag，Print 渲染时关闭；安全边距线已是 Preview-only（合规）。

### 12.6 下一步（仍不动代码）
1. **证伪 Gate A 的 canvas 一致性**：取一次 merge2/3/4 的实际打印 PNG（探针 B-PNG 已输出 `pngBytes`；可临时加一行把 PNG 存盘/base64 预览），与预览 canvas 并排比对 slot 矩形。
2. 若 canvas 一致但打印纸不 fit ⇒ 进 **P1**：查 `pyscripts/pdf_tool.py` 的 `batch-png-to-pdf` / `png_to_pdf_with_margin` 是否按 canvas 实际尺寸与方向生成 PDF 页（而非默认 A4 / 默认方向）——这正是原 H1。
3. **Gate B 独立修复**：分割线需单独 PR 引入 flag 关闭（不与本次混批、不 amend Commit A）。

### 12.7 纪律重申
- 不 amend `98a865fb`；不回头改 Commit A。
- D1（`isPrint` cache key）/ D2（`userMargins`）仍留 P2 **架构债务**——但 **D2 作为"fit 分叉原因"已被代码排除**，仅 cache key 串味风险保留。
- 修复验证通过后，P0 探针单独 Commit B 清理。

---

## 十三、Stage 2 收口 + P1-A / Gate B 双轨正式化（2026-08-14 21:4x）

用户确认 Stage 2 可收口，结论升级：**"fit 不一致"嫌疑从前端渲染层被推回 P1 下游**；下一步严格分为
**P1-A（pdf 管线：Canvas 对、纸张不对）** 与 **Gate B（artifact 不含辅助线）** 两条，禁止混修。

### 13.1 Stage 2 收口声明（代码级已证）
| 判定项 | 结论 | 依据 |
| --- | --- | --- |
| Preview / Print Canvas 几何一致 | ✅ 已证 | 同一 `computePaperLayout` + 同一 `usableRect` + 同一 Worker + 同一 V16 `MultiTicketComposer`；`userMargins` 零引用（`§12.1–12.2`） |
| fit 分叉在渲染层 | ❌ 排除 | `§12.4`：分叉既非 `userMargins`、也非 `paperLayout` 构造 |
| fit 分叉位置 | → **P1 下游** | `PNG → batch-png-to-pdf → PDF MediaBox → Sumatra`，即原 H1（因 P0 通过而重回候选首位） |
| 分割线进 artifact | ⚠️ **违规确定** | `render.worker.js:35-71` 无条件绘制（Gate B，不依赖 P1 结果即可定性） |
| 安全边距线进 artifact | ❌ 代码排除 | 仅在 `PrintPreviewCanvas.jsx:405-417` SVG overlay；Worker 无此分支（`§12.3`） |

⇒ Stage 2 渲染层审查结束。剩余未知全部落在 **P1 实证**，而 P1 的"入场证据"就是**实际生成的 merge PNG**（P1-1）。

### 13.2 P1-A：pdf 管线取证（Canvas 对，纸张不对）

#### 13.2.1 核心代码实证 —— MediaBox 的唯一来源是 PNG 像素
`electron/main.js:935-938` 调 Python：
```js
const batchResult = await callPython([
  'batch-png-to-pdf',
  JSON.stringify({ files: batchFiles, margins, dpi: 300 }),
])
```
**注意：只传了 `margins` 与 `dpi:300`，没有传任何"目标纸张尺寸"。**

`pyscripts/pdf_tool.py:28-31`（`png_to_pdf_with_margin` 的 `_layout`）：
```python
def _layout(w_px, h_px, ndpi):
    w_pt = w_px * 72 / dpi
    h_pt = h_px * 72 / dpi
    return (w_pt, h_pt, w_pt, h_pt)
pdf_bytes = img2pdf.convert(input_png, layout_fun=_layout)
```
⇒ **生成的 PDF MediaBox = PNG 像素尺寸 × 72 / 300（dpi）**。
纸张大小既不是输入，也没有任何"与 target paper 比对"的显式校验。

#### 13.2.2 冻结 Margin Contract 的"隐性依赖"（关键）
冻结契约：**MediaBox == target paper**。
代码层面该契约**仅在以下隐含条件成立时才满足**：
```
PNG 像素尺寸 == 目标纸张(mm) × (300 / 25.4)   （每轴）
```
即：**纸张正确性被完全委托给 PNG 的像素尺寸**。
- 若 print canvas 确实按 `computePaperLayout` 的纸张 @300 DPI 出图 → MediaBox == paper ✅ 契约成立；
- 若 PNG 像素尺寸偏差（DPI 错、纵横比错、被裁成 content-only bbox）→ MediaBox **静默偏离** paper ❌ 契约破坏，**无任何报错**。

这正是"Canvas 对、纸张不对"的代码级解释：**PNG 像素尺寸就是纸的对错判据**，而下游 PDF 只是忠实复刻 PNG。

#### 13.2.3 边际扩展（double-margin 嫌疑，P1-2 第二候选）
`pdf_tool.py:42-69`：当 `extractMargins(settings)` 返回非 0 时，**MediaBox 被"长大"**（四边外扩 `mm_to_pt(margin)`）。
```python
if L or R or T or B:
    for page in pdf.pages:
        mb = page.mediabox
        page.mediabox = [mb[0]-dl, mb[1]-db, mb[2]+dr, mb[3]+dt]
```
⇒ 最终 PDF 页 = PNG 尺寸 + 边距。若 Sumatra 以 noscale/actual-size 提交到"纸 = (纸+边距)"，会出现
"内容偏移 / 看似缩小未贴边"的典型症状。这与 `c2-phase1b-step4-margin-dependency-audit.md §3.5` 的
"疑似双重边距"指向同一处。是否真 double，需看 `pdfMargin.extractMargins` 实际返回值（P1-2 第二项）。

#### 13.2.4 无 `/Rotate` 写入（orientation 责任上移）
`pdf_tool.py:44` 仅 **读取** 已有 `/Rotate`（img2pdf 生成的 PDF 无 Rotate → 视为 0），**从不写入** Rotate。
⇒ 横向（merge4）的正确性完全依赖：**canvas 在渲染期已被正确旋转/尺寸交换**，或 `DirectPrintHandler`/Sumatra
侧处理 orientation。PDF 生成这一步不修正方向。

#### 13.2.5 P1 取证顺序（继续"节点失败即短路"）
| 节点 | 检查项 | 现有日志锚点 | 短路判定 |
| --- | --- | --- | --- |
| **P1-1 Canvas / PNG** | PNG 像素尺寸(W×H)、方向、内容 | `main.js:925` PNG `buf` 字节；**缺像素 W/H**（见 13.2.6） | PNG 尺寸≠纸@300dpi ⇒ P1 退回渲染层 |
| **P1-2 batch-png-to-pdf** | **MediaBox** / Rotate / page count / margins | `main.js:960-961` `extractMediaBoxAsync` → `MediaBox=...`（已埋点！） | MediaBox≠纸 ⇒ 根因在此（原 H1） |
| P1-3 DirectPrintHandler | 实际提交 PDF / printer / orientation / fit 参数 | `DirectPrintHandler.handle` | 提交参数错 ⇒ 此处 |
| P1-4 Sumatra | exit code / stderr | spawn 日志 | 非零 ⇒ Sumatra 侧 |

**P1-2 是当前最高价值节点**——但注意它与 P1-1 **强耦合**：`MediaBox = PNG_px × 72/300`，
所以**取一次实际 merge PNG 并读出其像素 W/H，即可同时判定 P1-1 与 P1-2**。这正是"先拿实际 PNG"的决定性意义。

#### 13.2.6 立即补一个最小探针（仍属"取证"，非修复）
当前 `main.js:925` 只记 PNG `buf` 字节、`:961` 记 `MediaBox`（pt），**二者都缺 PNG 像素尺寸**。
**已选 Path B（2026-08-14 21:5x）**：在 `main.js` `written` 完成后、`batch-png-to-pdf` 之前插入 `[P1-1][CAPTURE]` 抓取探针，把**真正送入 P1-2 的原始 PNG** 持久化到 `D:\marsprint\print703\_png_capture\`（仅 `copyFileSync` + 打印路径，不改 PNG/PDF/MediaBox/Sumatra、不碰 `finally`、不进 Commit A；`node --check` 已通过）。取到 PNG 后由 agent 直接 `Read` 读真 W×H 并比对预览（取证后查四件事：① PNG 实际 W×H ②纸张边界与票据内容像素位置 ③是否达预览 fit ④ orientation 是否正确），再与生成 PDF 的 `MediaBox / /Rotate / page size` 逐一比较。
（备选内联日志，若不想落盘）：
```js
const { width, height } = sizeOfPng(buf)   // 或靠 pdf_tool.py 回传像素
console.log('[print-merged-images] PNG %d pixel: %dx%d (expect paper@300dpi)', i+1, width, height)
```
Expectation（冻结 V16：PREVIEW_DPI=EXPORT_DPI=300）：A4 = `2480×3508`、A4 横 = `3508×2480`。
若实测 pixels 偏离该值 ⇒ Canvas 尺寸本身错（P1 退回渲染）；若命中 ⇒ MediaBox 必 = paper，根因在 P1-3/P1-4。

### 13.3 Gate B：独立确定性违规，延后单独批
- **定性（不依赖 P1）**：`render.worker.js:35-71` 在 `slots.length>1` 时**无条件**把分割线（`#cccccc` 虚线）
  画进 Final Canvas → 它既是 Preview 内容、也是 Print artifact。属 **Preview/Debug Guide 误写入 Final Render Artifact**。
- **修复方向（用户认可）**：引入显式语义参数，而非 `if (isPrint) { 不画线 }`：
  ```
  Render
   ├── content
   ├── slot geometry
   └── optional guides  ── Preview → true / Print → false / Debug → 可配置
  ```
  语义边界：`showGuides`（或 `includeSeparators`）独立控制辅助线；未来 Preview 分割线 / Debug 安全边界 /
  导出纯净图 / 打印纯净 PDF 各有明确开关。
- **提交纪律**：Gate B 单独 PR/Commit，**不 amend Commit A、不与 P1 混批**；等 **Gate A（P1-1/P1-2）收口后**再开修复批次。
- **安全边距线：暂不改**。`PrintPreviewCanvas.jsx:405-417` 的 safe-margin 线仅 SVG overlay，Worker 无绘制分支
  （`§12.3` 代码排除）。若用户肉眼在打印件看到"安全边距线"，须先经 **P1 取证（实际 PNG / PDF）**确认是哪条线，
  **禁止**因肉眼一条线就改 safe-margin rendering——否则会把"实际分割线"误判成"safe-margin line"。

### 13.4 当前状态图（用户给定，转述）
```
Commit A  read-file IPC contract 修复
    │
    ▼  P0 PASS
┌───────────────────────────┐
│ Stage 2                   │
│ Preview/Print 几何        │
│ code-level equivalence    │
└─────────────┬─────────────┘
              │
       ┌──────┴──────┐
       ▼             ▼
    Gate A          Gate B
       │             │
       │             └─ Worker 分割线：已确定违规（延后单独批）
       │
       └─ PNG 实物待验证（P1-1）
              │
              ▼
       P1 PDF Pipeline
              │
              ▼
       MediaBox / Rotate / margins   ← P1-2 最高价值（MediaBox=PNG_px×72/300）
              │
              ▼
           Sumatra
```

### 13.5 即时下一步（唯一动作）
**先拿一次实际生成的 merge PNG（merge2/3/4 各至少一例）**，读出其像素 W×H 并与"纸@300dpi"期望值比对：
- PNG 尺寸已命中预期 ⇒ 渲染层与 MediaBox 均正确 → 直接进 `DirectPrintHandler`/Sumatra（P1-3/P1-4）；
- PNG 尺寸已偏离 ⇒ P1 立即退回渲染/Worker 层（与"Canvas 对"假设冲突，需重新审查 canvas 尺寸来源）。

分割线（Gate B）已是确定性违规，可独立排队，但按纪律**等 Gate A 收口后再开修复批次**，保持提交边界干净。

---

## 十四、P1-1 实物取证终审 + P1-2 双重边距实锤 + Gate B 实锤（2026-08-14 22:0x）

Path B 探针（`main.js` `written` 后 copyFileSync）已落地并复现 merge2/3/4，三张原始 PNG 落在 `D:\marsprint\print703\_png_capture\`。结论如下。

### 14.1 P1-1 实物证据（PNG 本身）

| 文件 | 像素 W×H | 方向 | 推断纸张@300dpi |
| --- | --- | --- | --- |
| `merge_1786716163656_page_1.png` | **1750×2500** | 纵向 | A5（1748×2480≈1750×2500）✅ |
| `merge_1786716195636_page_1.png` | **1750×2500** | 纵向 | A5 ✅ |
| `merge_1786716205324_page_1.png` | **2500×1750** | 横向 | A5 旋转（2480×1748≈2500×1750）✅ |

- **DPI 正确**（300）、**纸张正确**（A5，非 A4）、**方向正确**（merge2/3 纵、merge4 横）。
- PNG 由 `renderMultipleItemsToCanvas` → Worker 输出，内容按 `computePaperLayout` 的 `usableRect`（3mm 安全边距已烤进布局）摆放。

### 14.2 P1-2 双重边距 —— 已用日志精确反推坐实

用户日志 `MediaBox` + `margin fields L=3 R=3 T=3 B=3`：
```
MediaBox = PNG_px × 72/300  +  3mm margin on EACH of 4 sides
纵向: 420.00×600.00 (PNG pt)  →  437.01×617.01   (四边各 +8.50pt = 3.00mm)
横向: 600.00×420.00 (PNG pt)  →  617.01×437.01   (四边各 +8.50pt = 3.00mm)
```
与 `pdf_tool.py:28-31`（`_layout` 初值 = PNG_px×72/300）+ `:42-69`（margin>0 时四边外扩 `mm_to_pt(3)`）**逐字节吻合**。

**这就是"票据没有像预览一样 fit 到边距"的 P1 级根因**：
- Preview / PNG canvas：内容已按 3mm 安全边距 inset（usableRect）。
- `pdf_tool.py` 又按 `settings.margin=3mm` 把 MediaBox **四边再外扩 3mm** → 打印页 = A5 内容 + 6mm 等效边距。
- Sumatra 以 fit-to-page 提交 A5+6mm 图像到 A5 纸 → 整体缩放 ~95.9% + 内容再内移 3mm → 打印件内容比预览更靠内、更小。
- ⇒ **Canvas 正确，纸张（PDF MediaBox）错误** —— P1-A 假设（"Canvas 对、纸张不对"）**被实证证明成立**。

### 14.3 Gate A 终审判定（修订 §13.2.5 的"待验证"）

| 问题 | 判定 | 依据 |
| --- | --- | --- |
| PNG canvas 尺寸/方向 | ✅ 正确 | §14.1 |
| Preview canvas ≡ Print canvas（同一渲染函数、同一归一化 paperLayout） | ✅ 代码级一致 | `computePaperLayout.toPx` 把 `undefined` 与 `??3` 都归一为 3mm（§12.2），`usableRect` 逐字节相同 |
| 用户实测"PNG 与预览不一致" | ⇒ 指 **预览 vs 打印纸** | PNG canvas 本身 = 预览 canvas；差异被 P1-2 双重边距 + Sumatra 引入（下游，非渲染几何） |

**重要澄清**：若用户字面意思是"抓到的 PNG 与屏幕预览画布像素不同"，则可能存在**次级的 content-source 分叉**（Preview 用 `mergePair` 的 `_previewImageUrl`/预载，Print 用 `group` 的 `read-file` 原始数据，分辨率/来源不同导致 slot 内 fit 微差）。需用同一探针抓 Preview canvas 并像素 diff 才能 100% 坐实。但**主导且已坐实**的"不 fit 像预览"原因就是 P1-2。

### 14.4 Gate B 实物确证（独立、不依赖 P1）

三张 PNG 全部含 `#cccccc` 虚线分割线（`render.worker.js:35-71` `slots.length>1` 无条件画入 Final Canvas）。**已是 Preview/Debug Guide 误写入 Final Render Artifact 的确定性违规**。修复方向见 §13.3（`showGuides` 语义参数，Preview→true / Print→false），独立批次，不 amend Commit A、不与 P1 混批。

### 14.5 下一步修复批次（待用户授权，严格分两批）

**批次一 · P1-2（fit）**：`pdf_tool.py` 的 `png_to_pdf_with_margin` 对 merge/print 路径**不再按 `settings.margin` 外扩 MediaBox**（canvas 已烤好安全边距）。最小修复二选一：
- (a) `main.js:935-938` 调 Python 时传 `margins:{}`（仅 merge-print 路径），或
- (b) `pdf_tool.py:42` 增加开关：仅当调用方声明"canvas 未含边距"时才外扩。
修复后 MediaBox = 目标纸（A5 pt = 419.53×595.28），图像 1:1 映射到纸，内容保持 3mm，与预览一致。

**批次二 · Gate B（分割线）**：引入 `showGuides` 参数，`_renderDirect` 与 Worker `compositeCanvas` 的分割线绘制均受控（Preview=true / Print=false）。

**纪律**：两批独立 Commit；不 amend `98a865fb`；P0 探针待验证通过后单独 Commit B 清理；安全边距线（SVG overlay）仍不改。

### 14.6 批次一（P1-2）修复已落地（2026-08-14 22:1x，用户授权）

**边界（用户冻结，严格只动一处）**：仅改 `main.js` merge-print → `batch-png-to-pdf` 的调用参数；**不改 `pdf_tool.py` 全局语义**、不动 Preview/Canvas 几何、`computePaperLayout`/`computeSlots`/`MultiTicketComposer`、`Sumatra`/`DirectPrintHandler`/`isPrint`/`userMargins`/布局算法、不动分割线/安全边距线、不碰 P0 探针、不 amend `98a865fb`。

**改动**：`electron/main.js` `print-merged-images` 处理器内，原
```js
const margins = pdfMargin.extractMargins(settings)   // → {left:3,right:3,top:3,bottom:3} → Python 四边外扩 3mm
```
替换为
```js
// P1-2 修复（批次一，仅改此 merge-print 路径，不动 pdf_tool.py 全局语义 / 单票路径）：
// merge-print 的 Canvas 已通过 usableRect 烤入 3mm 安全边距，PNG 像素即目标纸张；
// 故 PDF 层禁止二次外扩 MediaBox，否则打印件比预览多一圈边距 + fit-to-page 整体缩小。
const margins = { left: 0, right: 0, top: 0, bottom: 0 }
```
`node --check` 已通过。

**机制确定性（已用日志反推坐实，非猜测）**：`pdf_tool.py:37-40` 读 `margins.get("left"/...)`，`:42` `if L or R or T or B` 为 false ⇒ `:54-59` 外扩不执行 ⇒ MediaBox == `_layout` 初值 == `PNG_px × 72/300` == A5 目标纸。即修复后：
```
当前（错）：  PNG 420×600 pt → MediaBox 437.01×617.01 pt（四边各 +3mm）
修复后（对）：PNG 420×600 pt → MediaBox 420.00×600.00 pt（1:1，无外扩）
```
（横：600×420 → 617×437 改 600×420；A5 标准 pt 419.53×595.28 取决于像素/DPI 精度，验收重点为"不再四边外扩 3mm"，不强求特定浮点。）

**验收 Gate（用户侧运行，仅测 P1-2，暂不处理分割线）**：merge2 / merge3 / merge4 各跑一例，确认
1. PNG 尺寸仍正确（A5@300dpi / 横）；
2. 主进程日志 `MediaBox=[...]` **不再比 PNG 对应纸张大 3mm×4 边**（纵向应 ≈420×600，横向 ≈600×420，误差在 DPI 取整内）；
3. 实际打印纸内容重新贴近预览边距（不再因二次 margin + fit 缩放而偏小/内移）。

验证通过后单独提交本批次为 **独立 Commit（不 amend A）**，P0/P1-1 探针留待 Commit B 清理。Gate B 仍独立排队。

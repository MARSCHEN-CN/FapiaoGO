# FapiaoGO 打包方案（Windows EXE）

> 目标：给出一份可直接照做的详尽打包方案。本文**只做方案，不执行打包**。
> 适用范围：Electron 43 + React 19 + Vite 8 前端 + Python 3.13 Flask 后端 + SumatraPDF 打印 + PDF.js 渲染。

---

## 0. 现状结论与关键风险速览

项目已有 `electron-builder.yml`，整体结构正确（前端进 asar、后端/资源进 extraResources）。但存在 4 个会导致打包后**功能缺失或图标不对**的阻断项，必须先行修复：

| # | 风险 | 影响 | 对应需求 |
|---|------|------|----------|
| R1 | `resources/icon.ico` 不存在 + `BrowserWindow` 未设 `icon` | EXE/安装包/任务栏/窗口仍是 Electron 原生图标 | 9 |
| R2 | `electron-builder.yml` `files` 排除全部 node_modules 后只回灌 electron-updater 系，**漏掉 archiver 与 exceljs** | 压缩包导出、Excel 导出（主进程逻辑）打包后失效 | 4 |
| R3 | `frontend/public/fonts` 未进 extraResources，且 OFD 渲染代码写死读取 `frontend/public/fonts/` | OFD 发票渲染在打包后报字体缺失 | 1、2 |
| R4 | 前端多处写死绝对根路径 `/icon/...`、`/fonts/...` | `file://` 下解析到盘根，图标/字体丢失 | 1、3 |

---

## 1. 整体打包架构

```
FapiaoGO Setup.exe (NSIS)
├─ resources/
│  ├─ app.asar            ← electron/main.js + preload + dist/(前端)   [asar 内]
│  ├─ backend/            ← 完整 venv + 源码 + models(OCR onnx)         [extraResources, asar 外]
│  ├─ pyscripts/          ← pdf_tool.py 等                              [extraResources]
│  ├─ sumatra/            ← SumatraPDF.exe                              [extraResources]
│  ├─ cmaps/              ← PDF.js 中文映射                             [extraResources]
│  ├─ standard_fonts/     ← PDF.js 标准字体                             [extraResources]
│  ├─ wasm/               ← PDF.js wasm 解码                            [extraResources]
│  └─ fonts/              ← OFD 渲染字体（需新增）                       [extraResources]
└─ FapiaoGO.exe
```

数据流：前端(`file://dist`) → IPC / HTTP `127.0.0.1:5000` → Python 后端(`backend/venv/python.exe app.py`) → 返回 PDF/Excel/Zip；打印走 SumatraPDF。

---

## 2. 图标 / 图片 / 字体（需求 1、9）

### 2.1 EXE / 安装包 / 任务栏图标（核心修复，需求 9）
- **现状**：`electron-builder.yml:47` 写 `win.icon: resources/icon.ico`，但仓库内**无任何 `.ico` 文件**（`resources/` 下仅有 `sumatra/`），故回退 Electron 默认图标；`main.js:118` 的 `BrowserWindow` 也未设置 `icon`。
- **修复步骤**：
  1. 用 `frontend/public/icon/app-icon.png` 生成多尺寸 `resources/icon.ico`（建议含 16/32/48/256，256 用 PNG 压缩层）。工具：`png-to-ico`、ImageMagick（`magick convert app-icon.png -define icon:auto-resize=16,32,48,256 icon.ico`）或在线生成器。
  2. `electron/main.js` 的 `BrowserWindow` 选项增加：
     ```js
     icon: path.join(__dirname, '../resources/icon.ico')
     ```
     并确认 `app.setAppUserModelId('com.FapiaoGO.app')`（appId 已配，确保任务栏正确分组）。
  3. 任务栏图标 = EXE 图标（由 `win.icon` 决定），修好 1/2 后即生效；若仍不显示，检查安装后的快捷方式是否指向新 exe。
  4. 若项目使用 Tray（系统托盘），其 `icon` 同样改用 `icon.ico`。
- **验证**：exe 文件图标、安装向导、任务栏、卸载程序图标、窗口标题栏均显示项目图标。

### 2.2 前端图片 / 图标资源
- `frontend/public/icon/` 下 22 个 svg/png（PDF/xlsx/zip/printer/rename/calculator/files/waiting 等）。
- **问题**：`ActionBar.jsx:94/101/108` 与 `DevDocumentViewerDemo.jsx` 写死 `/icon/PDF.svg` 等**绝对根路径**，`file://` 下失效（解析到 `file:///icon/...` = 盘根）。
- **修复**：统一改为 `PUBLIC_BASE + 'icon/PDF.svg'`（`PUBLIC_BASE = import.meta.env.BASE_URL`，`base:'./'` 下为相对）。`DevDocumentViewerDemo` 的 `/test/*.svg` 仅 dev demo，用 `import.meta.env.DEV` 守卫或随 `public/test` 一起排除。
- 空态/状态图（`waiting.svg`/`files.svg`/`nosearch.svg`/`wait.svg`）已通过 `PUBLIC_BASE` 引用，保留即可。

### 2.3 字体
- **UI 字体**（MiSans / NotoSansSymbols 四款 woff2，`public/fonts/`）：`src/index.css` 的 `@font-face` 与 `src/index.html` 的 `<link rel=preload>` 写死 `/fonts/...` → `file://` 失效。
  - **修复**：改为相对路径 / `PUBLIC_BASE`（Vite 会把 `public/fonts` 拷到 `dist/fonts`，相对引用即可加载）。
- **OFD 字体**（Python 读取，见 3.4），需单独进 extraResources。

---

## 3. 后端打包（需求 2）

### 3.1 当前机制
- **不**打包成单文件 exe，而是把整个 `backend/venv` + 源码 + `backend/models`（OCR onnx 模型）+ `pyscripts` 作为 extraResources 拷贝到 `resources/backend`。
- 主进程 spawn：`process.resourcesPath/backend/venv/Scripts/python.exe backend/app.py`，端口 5000（`app.py:1970`）。

### 3.2 必须随包分发的依赖
- **二进制（关键）**：numpy、opencv-python(5.0.0.93，是 rapidocr 传递依赖但**未列在 requirements.txt，务必保留**)、onnxruntime、PyMuPDF(fitz)、pikepdf、pillow、scikit-learn、cryptography、lxml、python-magic-bin（自带 libmagic DLL）。
- **纯 Python**：flask、flask-cors、pdfplumber、openpyxl、img2pdf、orjson、rapidocr、readerwriterlock、pyyaml。
- **可剔除**：`pdf2image`（后端未 import）、根目录 `.venv`（重复，仅用 `backend/venv`）、venv 内 `tests/`、`__pycache__/*.pyc`（extraResources filter 已排除 pyc/tests，需确认 `.venv` 不被误带）。

### 3.3 extraResources 已覆盖
- `backend`（整体，排除 pyc/tests）、`pyscripts`、`cmaps`、`standard_fonts`、`wasm`、`sumatra` —— ✅ 已配。
- OCR 模型 `backend/models/{det,rec,cls,keys}` 随 `backend/**` 自动包含 ✅（filter 不误伤 `.onnx/.txt`，需确认）。
- **缺口**：`frontend/public/fonts` 未加入 → OFD 字体缺失（见 3.4）。

### 3.4 OFD 字体缺口（重要，R3）
- `ofd_parser/ofd_constants.py:17` 写死指向 `frontend/public/fonts/`，打包后该路径不存在；`ofd_renderer.py` 还引用系统字体 simsun/arial/cour。
- **修复**：
  1. `electron-builder.yml` extraResources 增加：
     ```yaml
     - from: frontend/public/fonts
       to: fonts
     ```
  2. Python 侧字体目录解析改为：
     ```python
     base = os.environ.get('FAPIAO_RESOURCE_PATH') or os.path.dirname(sys.argv[0])
     font_dir = os.path.join(base, 'fonts')
     ```
     回退到系统字体。需在 `main.js` spawn 后端时把 `FAPIAO_RESOURCE_PATH` 透传进 env（类似现有 `FAPAIAO_RESOURCE_PATH`）。

### 3.5 运行时可写路径
- 确认后端**不写** `resourcesPath`（只读）。现状：DB → `~/.fapiaogo`，OCR 缓存 → `~/.fapiaogo/.ocr_cache` 或 TEMP，import 临时 → `%TEMP%`。
- **需验证**：`backend/logs/backend.log` 在 prod 下不能写 `resourcesPath`。`app.py:1932` 应有降级到 `userData/TEMP`，否则后端日志写入失败（不影响核心功能但丢失排障信息）。

### 3.6 体积与瘦身
- venv 含 numpy/opencv/scikit/onnxruntime，整体可能 600MB–1.2GB。建议：
  - 用 64 位 wheel；清理 pip cache、`.pyc`、`tests`、重复 `.venv`。
  - 第一版沿用「venv 整体拷贝」方案（最稳）；若需更小体积，后续可改用 PyInstaller 对 `backend/app.py` 打包（工作量更大，列为可选）。
- **单实例锁**：确保 `app.requestSingleInstanceLock()`，避免两个实例抢 5000 端口。

---

## 4. 前端打包（需求 3）

### 4.1 构建
- `cd frontend && npm install && npm run build` → 输出 `D:\marsprint\print703\dist`（Vite `base:'./'`、`outDir ../../dist`、`publicDir ../public`，已为 `file://` 适配）✅。
- `electron-builder.yml` `files` 含 `dist/**/*` ✅。

### 4.2 资源路径
- PDF.js 资源经 `window.electronAPI.resourcePath` → `file://resourcesPath/cmaps|standard_fonts|wasm/`（`renderers.js:61-66`，preload 注入 `FAPAIAO_RESOURCE_PATH`）✅。
- **问题**：前端 `/icon/*`、`/fonts/*` 绝对路径（见 2.2/2.3）→ 需改相对。
- **`process.env` 守卫**：浏览器侧 `useFileOps.js` 等用 `process.env.NODE_ENV`/`process.env.VITE_*` 无兜底，生产构建可能崩溃 → 统一改用 `import.meta.env.DEV` / 提供默认值。

### 4.3 构建产物校验
- 确认 `dist/index.html`、`dist/assets`、`dist/fonts`、`dist/icon` 都在；`dist` 不应含 `test/`。
- `index.html` 的 CSP 白名单与 `<link preconnect>` 写死 `http://localhost:5000`，与后端端口一致，保留即可（端口变动时需同步）。

---

## 5. 功能模块打包注意事项（需求 4–8）

| 模块 | 依赖 | 打包风险 / 处理 |
|------|------|----------------|
| 导出 PDF | 后端 `/api/export-pdf` + SSE（pdfplumber/PyMuPDF/pikepdf） | 后端在 extraResources ✅；确认后端依赖齐全 |
| 导出 Excel | 后端 `/api/export-excel-sse`（openpyxl）+ 主进程 exceljs | ⚠️ 主进程 exceljs 被 `files` 排除 → 必须补（R2） |
| 导出压缩包 | 主进程 `ipc-pack.js` + `archive-utils.js` 用 `archiver` | ⚠️ archiver 被 `files` 排除 → 必须补（R2） |
| 重命名 | 主进程 `ipc-rename.js`+`rename-utils.js`，前端 `useRenamePack.js` | 纯 JS，随主进程打包 ✅ |
| 打印 | 新管线 `PrintService`+`OsLauncherBridge`+SumatraPDF | SumatraPDF 在 extraResources ✅；确认 `resourcesPath/sumatra/SumatraPDF.exe` 解析正确 |
| 展示区 / 文件列表 | `Sidebar.jsx`/`FileList.jsx`/`DisplayAdapter.jsx`/`DocumentViewer.jsx` | 前端 ✅ |
| 打印预览区 | `PrintPreviewCanvas.jsx`/`PrintConfirmModal.jsx` | 前端 ✅（依赖 PDF.js 资源，见 4.2） |
| 统计区域 | `Sidebar.jsx` 统计 DOM + `excelTotals.js`/`documentViewModel.js` + 后端 statistics | 前端 + 后端 ✅ |
| 计算器 | `CalculatorModal.jsx`/`CalculatorWindow.jsx`，独立窗口 `#/calculator` | 前端 ✅ |
| 菜单 | `TopBarMenu.jsx`/`TopBar.jsx`；主进程右键菜单已禁用 | 前端 + main.js ✅ |

---

## 6. 完整打包命令序列（只给方案，不执行）

建议根 `package.json` 增加脚本：
```json
"scripts": {
  "pack:win": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix electron install --production && electron-builder --win --x64 --config electron-builder.yml"
}
```
手动分步：
```bash
# 1. 前端构建 → 根 dist/
cd D:\marsprint\print703\frontend
npm install && npm run build

# 2. 准备 electron 运行期依赖（关键修复 archiver/exceljs，R2）
cd D:\marsprint\print703\electron
npm install --production     # 或 npm prune --production

# 3.（若未生成）产出 resources/icon.ico（R1，见 2.1）

# 4. 打包
cd D:\marsprint\print703
npx electron-builder --win --x64 --config electron-builder.yml
# 输出 release/ 下：FapiaoGO Setup.exe (nsis) + FapiaoGO-portable.exe
```

### electron-builder.yml 需修改点（汇总）
- `win.icon` 指向**真实存在**的 `resources/icon.ico`（先生成文件，R1）。
- `files`：停止「全排除 node_modules 后只回灌 electron-updater」，改为保留生产依赖（前提是步骤 2 已 `npm install --production`）：
  ```yaml
  files:
    - "electron/**/*"
    - "dist/**/*"
    - "!electron/node_modules/.bin/**"
    - "!electron/node_modules/electron/**"
    - "!electron/node_modules/electron-builder/**"
    - "!electron/node_modules/@electron/rebuild/**"
    - "!electron/node_modules/electron-rebuild/**"
    - "!electron/node_modules/typescript/**"
  ```
- `extraResources` 增加 `frontend/public/fonts → fonts`（R3），并确认 `backend/models` 随 `backend/**` 自动含入。
- 可选：`fileAssociations` 注册 `.pdf`；`publish` 改为真实更新地址或暂时注释。

---

## 7. 其他遗漏与增强建议（需求 10）

- **代码签名**：未签名 EXE 在 Win10/11 触发 SmartScreen 拦截。需代码签名证书（DigiCert/GlobalSign 等），在 builder 配 `win.certificateSubjectName` 或 CI 签名。列为发布前置。
- **杀软误报**：Electron + Python OCR 二进制易触发 Defender/第三方 AV 误报，建议代码签名 + 提交白名单。
- **dev-only 资产剔除**：`frontend/public/test/` + `test.pdf` 仅 demo，应在 build 时排除（减小体积并避免 `/test/*.svg` 硬编码失效）。
- **Tray 图标**：若使用系统托盘，确认其 icon 也用项目图标。
- **版本 / 元数据**：builder 可加 `copyright`、`description`；version 用 `package.json` 1.0.0，建议与 Git tag 对齐。
- **更新地址 publish**：当前 `https://update.example.com/dev/` 为占位，首版可注释或填真实地址。
- **单实例 / 端口**：`app.requestSingleInstanceLock()` 确认，避免重复启动抢 5000。
- **运行期日志路径**：确认 `backend.log` 不写 `resourcesPath`（只读），改 `userData/TEMP`。
- **SumatraPDF 合规**：SumatraPDF 为 GPL 授权，分发需注意许可证合规。
- **架构一致**：`--x64` 构建时确保 `backend/venv` 也是 64 位 Python，否则运行时报平台不匹配。
- **安装目录写权限**：默认装 `Program Files`（`resourcesPath` 只读），后端写用户目录，OK；用户选其他目录同理。

---

## 8. 打包后验证清单

- [ ] exe 图标、安装向导、任务栏、卸载程序图标均为项目图标（R1）
- [ ] 启动无白屏，前端加载 `dist/index.html`
- [ ] 后端 5000 端口拉起，`/health` 返回 200
- [ ] PDF / Excel / 压缩包导出均成功（验证 archiver/exceljs/后端依赖，R2）
- [ ] OFD 发票渲染字体正常（fonts 进 extraResources + Python 路径修正，R3）
- [ ] PDF.js cmaps/standard_fonts/wasm 加载正常（中文发票不乱码）
- [ ] 打印经 SumatraPDF 出纸正常
- [ ] 重命名、统计、计算器、菜单功能正常
- [ ] 前端图标/字体在 `file://` 下不丢失（绝对路径已改相对，R4）
- [ ] 单实例、端口不冲突

# 打印预览方向切换内容旋转 — 调查与方案

> 日期：2026-08-04 · 状态：调查完成，待用户裁决 · 性质：方案（未改代码）

## 1. 问题

打印确认页里「方向」在横向 ↔ 纵向切换时，**内容不跟着旋转**——只有纸面 swap、票位重算，缩略图内容本身姿态不变（等比缩放换容器）。用户期望：**横向切换时内容跟着旋转 90°**（像真实打印那样）。

## 2. 现状调查（代码证据）

### 2.1 三条链的方向行为对照

| 链路 | 方向按钮(settings.landscape) | 内容旋转 | 证据 |
|---|---|---|---|
| **预览**（PrintPreviewModel → PrintPreviewCanvas） | ✅ 生效：横向可用区重算票位 + paperSizeMM swap | ❌ 不旋转：SlotImage 只按 slot fit（`xMidYMid meet`），rotation 仅来自 fileRotations | `PrintPreviewModel.js` pageToModel（0bd52b96 后横向物理可用区重算）、`PrintPreviewCanvas.jsx` SlotImage |
| **source 打印**（Sumatra 直送） | ❌ **无效**：Sumatra 不消费 landscape，纸方向 = 纸型固有方向 | rotate 由 `resolveOrientationCommands(内容方向, 纸型方向, 文件rotation)` 决定（旋转 90° 等） | `electron/print-service/print-settings.js` `getPaperOrientation`（A4→portrait 硬编码）；`usePrint.js` buildPrintSettings 无 landscape 字段 |
| **merge 打印**（canvas/compose） | ✅ 生效：`getForcedLandscape` → canvas 纸横向 | ❌ 不旋转：内容 fit 横向纸，rotation 仅来自 fileRotations | `renderers.js` isLandscape → createLayout；`usePrint.js` doPrint |

### 2.2 Sumatra 真实旋转语义（source 打印的事实源）

`electron/print-service/print-settings.js`：

```js
resolveOrientationCommands(contentOrient, paperOrient, desiredRotation):
  baseFlag = (contentOrient==='landscape') === isEven ? 'landscape' : 'disable-auto-rotation'
  ROTATE_LOOKUP = {
    'landscape|portrait':  {0:0, 90:90, 180:180, 270:270},
    'landscape|landscape': {0:90, 90:180, 180:270, 270:0},
    'portrait|portrait':   {0:0, 90:0, 180:180, 270:180},
    'portrait|landscape':  {0:90, 90:90, 180:270, 270:270},
  }
```

- **paperOrient 来自 `getPaperOrientation(纸型)`**：A4/Letter → portrait 固定；Voucher240x140 → landscape 固定。**与确认页方向按钮无关**。
- 关键格：`'portrait|landscape': {0: 90}`、`'landscape|landscape': {0: 90}` —— **横向纸时内容 rotate=90**（Sumatra rotate 顺时针，待真机复核方向）。

### 2.3 根因

1. **预览没有「内容方向」概念**：方向切换只换纸（swap + 票位重算），不模拟 Sumatra 的「内容 × 纸方向 → rotate」适配 → 内容不跟着转。
2. **更深层不一致（既有问题）**：确认页方向按钮对 **source 打印无效**（Sumatra 用纸型固有方向）——方向按钮调了，预览变、merge 打印变、**source 打印不变**。三岔口。
3. merge 打印：方向生效但内容不旋转（canvas fit 语义），与 Sumatra 语义也不同。

## 3. 方案设计

### 3.0 语义定义（需用户确认）

**方向按钮 = 纸张方向**。内容姿态按「纸张方向 × 内容方向」旋转适配（对齐 Sumatra rotate 语义）。这属于**打印姿态模拟**，不是展示区旋转泄漏——与既有冻结边界「打印预览无 contentRotation 权限」的关系需要重新界定（详见 §5）。

### 3.1 阶段 1：预览方向旋转适配（最小，先做）

- `PrintPreviewModel`：从文件元数据读取内容方向（`_pdfPageWidth/_pdfPageHeight`、`_imageWidth/_imageHeight`，与 `detectDocumentOrientation` 同源），每个 slot 增加方向适配旋转：
  - 横向纸 → 内容顺时针旋转 90°（`rotate=90` 语义）
  - 纵向纸 → 不旋转
- `PrintPreviewCanvas` SlotImage：旋转后 fit（先旋转再等比 fit 进 slot，与 `createPlacement` 同构；当前是 fit 后 `transform` 绕槽心转，会溢出槽位框——需改）。复用文件旋转（fileRotations）叠加：方向旋转 + 文件旋转合成。
- 测试：新增数值锚点（横向 A4 + 纵向内容 → slot 旋转 90° 后 fit 几何）。
- **不做**：不动打印，不引入 `resolveOrientationCommands` 前端副本（避免两套旋转源）。

### 3.2 阶段 2：source 打印方向按钮接入（修三岔口）

- `usePrint.js` source 路径：把 `settings.landscape` 传入打印管线（buildPrintSettings 增加字段）。
- 主进程 `print-settings.js`：landscape 时映射为纸张方向（或 desiredRotation），使 Sumatra 输出与预览一致（如 landscape → `rotate=90` 或 landscape flag）。
- 目标：**方向按钮对 source 打印生效**——调横向，Sumatra 内容旋转 90° 打印。
- 需真机验证 Sumatra 行为（rotate 方向、landscape flag 与纸型方向的交互）。

### 3.3 阶段 3：merge 打印内容旋转统一（可选，最复杂）

- canvas/compose 路径：方向旋转接入内容（createLayout / MultiTicketComposer 的 rotation 输入增加方向分量）。
- 风险最高，建议阶段 2 验证后再评估是否必要（merge 打印当前行为是否被用户接受）。

### 3.4 建议实施顺序

```
阶段 1（预览旋转适配 + 测试）→ 阶段 2（source 打印接入 landscape + 真机验证）→ 阶段 3 评估
```

每阶段独立 commit，符合 bisect 纪律。阶段 1 不依赖阶段 2/3，可立即开工。

## 4. 待验证项

| 项 | 验证方式 |
|---|---|
| Sumatra `rotate=90` 旋转方向（顺时针？） | 真机打印横向纸对比预览 |
| `resolveOrientationCommands` 表与预览旋转的一致性 | 阶段 2 真机 |
| 内容方向检测可靠性（`_pdfPageWidth` 在导入链路是否总是填充） | 代码审查 + 抽样 |
| merge 打印是否也需要内容旋转 | 用户确认业务语义 |

## 5. 冻结边界影响（需用户裁决）

- 既有裁定：「打印预览没有 contentRotation 权限，只有 paper orientation」。
- 本方案新增语义：**方向按钮驱动的内容适配旋转**（横向纸 → 内容转 90°）属于「打印姿态模拟」——它由纸张方向推导，不由展示区/文件旋转驱动，与既有边界不冲突，但需要明确写入冻结文档：
  - 预览权限：纸张方向 + 方向适配旋转（模拟 Sumatra）
  - 预览无权限：任意内容旋转（仍属 fileRotations）、展示区旋转
- 与 P4 已知 limitation（fit 后 rotate vs rotate 后 fit）交互：方向适配旋转必须「先旋转后 fit」——阶段 1 改 SlotImage 时顺带把文件旋转也统一为「先旋转后 fit」，P4 的部分问题随之消解。

## 6. 相关文件

| 文件 | 改动点（阶段 1） |
|---|---|
| `frontend/src/print/PrintPreviewModel.js` | slot 增加方向适配旋转（读内容方向） |
| `frontend/src/components/PrintPreviewCanvas.jsx` | SlotImage 先旋转后 fit |
| `frontend/test/printPreviewModel.test.mjs` | 新增方向旋转锚点 |
| `frontend/src/utils/detectOrientation.js` | 复用（不修改） |
